# Existing native declarations PR: prepared dependency join

Continue existing PR5789, “refactor(ir): share native resource declarations
before allocation”. Join published5404151bfc1b49d6cffed4a87a8985c51bd3dd93
with immediate prepared parent4bbdc154b7d31642d101803b367cb784c868c914.
All eight published production files remain byte-identical. Both complete
issue histories and the original published checkpoint handoff are preserved.
New migration scope remains paused. Keep this existing PR held until the
5759→5760→5787 predecessors are verified delivered through the protected queue.

The cumulative policy retains58 records: two combined snapshots, the exact
two published declaration records and the exact54 parent records. All allowed
edges are unchanged. The actual closure contains101 modules and374 imports:
240 type-only and134 runtime, with no graph errors. The test retains all257
parent controls and adds13 declaration controls; the complete270 cases pass.

The strict layout receipt now first authenticates seven independently
published forward spans between ea0f05c and5404151. It then applies the
unchanged original ten-statement, eight-factory and AST-payload inverse.
Fixture SHA256:1edabade58d599f6c2ca2941879833132c6a50ca4f81b6df546e66022eedc9d1.
All51 prior donor case intents survive; four mutations now target the actual
shape owners and the live unbound-context check asserts its earlier span
refusal. A new direct positive-first historical AST collision check preserves
the original guard. The13 prepared-registry case bodies and all old fixture
hashes remain exact. Sixteen new controls make67/67 donor assertions.

Final current-source evidence is1,070/1,070 assertions across17 complete files,
zero skips, assembled from separate serial complete-file executions. Preserve
the initial80/100 failure and composed336/337 provisional-census failure.
The exact per-file source/report hashes are in the owned ignored artifact
.tmp/5789-queue-drain/final-validation-20260913.json. The temporary one-case
graph observation was removed before the final complete boundary execution.

TS7, LOC/function budgets, oracle and preservation-v1 with required core types
and nodes passed. The unchanged coercion tool through a space-free alias
measured124 files/511 sites; the exact comparison base is4bbdc154. Ordinary
spaced-path empty scans receive no credit. Conformance synchronization changed
zero files. The independent test262 checkout uses pinb363f29d and canonical
53,933 test/harness blobs; original ambient links and fixtures remain intact.
Normal signed hooks and exact-head protected queue checks remain required.

Numeric precision debt, historical compiler-pair execution, physical async
acceptance, whole-program consumer completion and frontend retirement remain
uncertified. Next existing PR5792 adds one ref.as_non_null before struct.new;
preserve this join’s emitter imports. Its reviewed focused population is31
assertions across three files, plus canonical source gates. Do not substitute
unpublished later migration work or open duplicate PRs.
