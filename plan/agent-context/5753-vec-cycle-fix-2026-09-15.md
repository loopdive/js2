# PR5753 bounded vec Proxy cycle repair — acceptance and amendment receipt

Current disposition: parent-authorized Array query amendment implemented;
new O0/O2 controls **2/2 pass**, existing vec controls **4/4 pass**, final
original CI-harness fixtures **2/2 pass** (both strict variants). The historical
blocked-stage evidence below is retained, not rewritten as a green run.

Authoring agent: Codex, GPT-6 Astra Low. Parent integration owns publication.

Worktree: `/private/tmp/js2-5753-vec-cycle-fix-20260915`.
Branch: `codex/5753-vec-cycle-fix-20260915`.
Base/HEAD (no commit): `cb07815129e65312bc8f02675644146318e1ce49`.
Production write: `src/codegen/vec-prototype.ts` only. Additive test:
`tests/issue-5753-vec-proxy-cycle.test.ts`. This receipt is the third owned file.

## Implemented, not committed

Resolve existing `ctx.objectRuntimeTypes.proxyTypeIdx` after the existing helper
availability check; fail explicitly if the reserved layout is absent/negative.
After null exit and receiver-identity cycle rejection, emit fresh instructions:
local.get p / any.convert_extern / ref.test actual Proxy / br_if 1. Retain the
existing subsequent ordinary getPrototypeOf traversal and success/store path.
No new runtime resources, generic predicates, unwrapping, revocation checks,
trap calls, or edits outside the approved production owner.

## Original fixture paired results

Actual corpus repository top level `/Users/thomas/Code/js2/test262`; actual git
directory `/Users/thomas/Code/js2/.git/modules/test262`. Archive pin
`b363f29d3c43c626dc852744ad64a0b48a003693`, full harness plus ONLY:

- `test/built-ins/Array/prototype/indexOf/calls-only-has-on-prototype-after-length-zeroed.js`
  blob `436f32bf06bb1eaddf85c8dba407ac15119f6573`.
- `test/built-ins/Array/prototype/lastIndexOf/calls-only-has-on-prototype-after-length-zeroed.js`
  blob `c9101a4e20e5e9f26594a4c6b9c037bdc2665ff5`.

Extracted file hashes equal those literal pinned blobs. No dirty corpus copy.
Base is private git archive of `e9b43d3325352435aa7f84185ccb308c52ab909b`
under `.tmp/base`, with its own freshly built compiler/runtime bundles and
separately extracted pinned corpus. Candidate/fixed bundles built freshly in
this worktree. Only node_modules is shared read-only.

Parent's CI-harness command: pool4, fork/worker1024MiB, standalone,
proposals1, QuickJS, fullruntimeeval1, IR_FIRST/layout empty, chunk0/1,
IT timeout300000; original unified-worker path. Unique result prefixes and
timestamps `5753-vec-base`, `5753-vec-candidate`, `5753-vec-fixed`.
Node **22.23.2** on all three, NOT CI **25.9.0** runtime parity.

- e9 base: **2/2 pass**, both rows strict=both.
- unmodified cb: **0/2 pass**, both exact `Test262Error: [[GetPrototypeOf]] trap called`.
- fixed cb: **2/2 pass**, both rows strict=both.

The runner performs strict rerun only after primary success; candidate primary
failures therefore do not establish an independently executed strict failure.
Base/fixed successes cover both variants. Logs `.tmp/originals-{base,candidate,fixed}.log`;
full JSONL under each snapshot's `benchmarks/results/test262-5753-vec-*-results-*.jsonl`.

## Focused controls expose an additional blocker

Tests first: unmodified cb new O0/O2 cases **0/2**, throwing during install.
Fixed existing `issue-1058-vec-prototype-storage.test.ts`: **4/4 pass**.
Fixed new unannotated numeric-array O0/O2 cases: **0/2**, with **11 failed
prototype-value/identity assertions per optimization level**, all retained.
Soft assertions collect the full controls; they do not waive failures.

Installation does not invoke Proxy traps; explicit Proxy query still throws and
increments once. Direct/indirect cycle calls throw. Nonextensible same-prototype
succeeds; different/null/Proxy changes throw without consulting Proxy. Revoked
Proxy installation completes and explicit query still throws. However public
Object.getPrototypeOf numeric-array queries do not report installed overrides,
so identity and unchanged-state acceptance checks remain red.

Actual emitted O0 body preserved in `.tmp/selected-body.wat.log` (contains NUL;
use text-aware searches). `__object_setPrototypeOf_status` at lines1662 onward:
null exit1708-1710, identity1711-1723, actual Proxy type80 stop1724-1727,
ordinary getProto call204 at1728-1730, existing success1734-1735.
`directIdentity` at1129 instead constructs/reads the Array prototype singleton;
it does not call the prototype-store query. Source owner
`src/codegen/expressions/object-get-prototype-of.ts:431-434` maps declared Array
to its intrinsic prototype without checking the override. This is OUTSIDE the
approved production write set. No cast/any annotation or substituted query was
used to hide this failed acceptance check.

Logs `.tmp/focused-before.log`, `.tmp/focused-after.log`,
`.tmp/focused-after-split.log`, `.tmp/focused-after-all-controls.log` preserve
every stage. No timers enlarged, no original source/harness edits.

## Disposition

No commit because own acceptance controls are red. Parent/High must determine
the scoped follow-up for the distinct static prototype query owner; no authority
inferred to edit it. No push/PR/merge/queue actions. This repairs the demonstrated
trap in 2 of 137 losses, not the full standalone floor: original evidence remains
137 losses / 31 gains / net -106, and the published green regression gate covered
host only. Author config verified Thomas Tränkler <git@thomas.traenkler.com>;
future commit must use normal hooks and Codex co-author, after acceptance passes.

Pre-commit diagnostics completed: typecheck, focused-source Prettier check,
LOC budget, function budget, and git diff --check all pass. Budget scripts used
their normal upstream merge-base scope (113 changed source files), not a newly
relaxed allowance. Normal LFS-aware git status confirms exactly the three owned
files above; no other tracked changes. Hooks have not been invoked because no
commit is permitted while the new acceptance tests fail.

## Approved amendment and final verification

Parent approved one additional production owner:
`src/codegen/expressions/object-get-prototype-of.ts`. Its three Array folds now
share a native-provider-only override-aware read. Existing host-provider fold
is preserved. Receiver is evaluated once and saved; existing has/get helpers
distinguish explicit null from absence, with intrinsic Array.prototype fallback.
Resolve helper indices after receiver/fallback compilation and flushes. No new
runtime helper/type/global/import or generic MOP dispatch is introduced. Parent
also approved excluding Array from the earlier non-extensible plain-object
fold, so that it reaches the same Array read; typed-array branches are unchanged.

All original 22 failing O0/O2 assertions remain. Added fresh-returned-array and
side-effecting literal evaluation/default identity controls and two diagnostic
non-extensible checkpoints per optimization level. Final new tests **2/2 pass**,
zero module imports at both O0/O2. Existing vec test **4/4 pass**. Additional
carrier suite issue-5325 **7/7 pass**. Related ES5 issue-1472 **7/8** on fixed
AND untouched e9 base: exact same standalone intrinsic-relations test fails
because its harness supplies no `js2wasm:runtime-eval` import. No test/harness was
edited or filtered to conceal that baseline failure. Full final targeted run
is therefore **20/21**, not 21/21. Logs `.tmp/focused-final.log` and
`.tmp/related-base.log`; intermediate native-provider-guard diagnostic failure
remains in `.tmp/related-array-read.log`.

Fresh final bundles and original pinned2 rerun: **2/2 pass**, strict=both;
`.tmp/originals-final.log` and
`benchmarks/results/test262-5753-vec-final-results-5753-vec-final.jsonl`.
Earlier A/B and first fixed runs remain untouched. No broader floor credit.

Actual final emitted body in `.tmp/selected-body-final.wat.log`: directIdentity
now saves its actual receiver and calls current has/get helpers (120/121 in this
artifact), retaining the intrinsic fallback. Earlier failed singleton body
remains separately preserved. The compiled O0/O2 binaries both have no imports.
Final typecheck and normal LOC/function budget scripts pass, no allowances edited.
Commit uses normal hooks, Thomas author and Codex co-author; hook outcome is
reported with the resulting SHA to parent (no push).

High's final lane review identified `gc/native-first`, which does not reserve
the vec store. Parent approved guarding on standalone/WASI as well as native
provider; other lanes keep the prior fold. The paired gc/native-first query on
e9 and fixed both refuses exactly `env::__get_builtin` under native-provider
policy (logs `.tmp/gc-native-{base,fixed}.log`), not a missing-store error.
The new control asserts that exact baseline policy refusal; no successful GC
execution is claimed. Original standalone assertions remain unchanged.

First normal commit attempt passed lint-staged, LOC/function budgets and oracle
ratchet; changed-root hook selected 96 inherited files and skipped under its
existing >20 policy. Commit-message hook correctly refused the missing Model
trailer, so no commit was created. Retried message includes
`Model: Codex GPT-6 Astra Low`; no hooks skipped or gate settings changed.

Complete post-guard scoped run: **7/7 pass** (new3 + existing4),
`.tmp/focused-complete.log`. Complete original rerun **2/2 pass**, strict=both,
`.tmp/originals-complete.log` and unique `test262-5753-vec-complete` JSONL.
Post-guard typecheck and formatting pass. The separate related suite's
preexisting 1/8 runtime-eval-harness failure remains disclosed above.
