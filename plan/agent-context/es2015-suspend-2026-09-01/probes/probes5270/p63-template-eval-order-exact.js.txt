/*---
description: probe
includes: []
flags: [noStrict]
---*/
var tag = function(templateObject, a, b, c) { callCount++; assert.sameValue(a, 0, 'a'); assert.sameValue(b, 1, 'b'); assert.sameValue(c, 2, 'c'); };
var i = 0; var callCount;
assert.sameValue(`a${ i++ }b${ i++ }c${ i++ }d`, 'a0b1c2d');
i = 0; callCount = 0;
tag`a${ i++ }b${ i++ }c${ i++ }d`;
assert.sameValue(callCount, 1);
