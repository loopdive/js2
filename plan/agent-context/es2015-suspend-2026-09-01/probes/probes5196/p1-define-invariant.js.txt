// §10.5.6 step 16: trap says true, target has non-configurable non-writable foo:1 → TypeError
var target = {};
var p = new Proxy(target, { defineProperty: function(t, prop, desc) { return true; } });
Object.defineProperty(target, "foo", { value: 1 });
var threw = false;
try { Object.defineProperty(p, "foo", { value: 2 }); } catch (e) { threw = e instanceof TypeError; }
if (!threw) throw new Error("no TypeError from defineProperty invariant");
