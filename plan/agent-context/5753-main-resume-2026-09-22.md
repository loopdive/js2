# September 22 IR checkpoint continuation

Starting published checkpoint: `cddba56b768f30eb5d9af29d2954dd69e2b534b5`.
Fetched loopdive/js2 main: `6190e96105bb1168607fbcfe252b83a26f9eca50`.
Persistent isolated branch: `codex/5753-resume-20260922`.
The dirty shared main checkout and old temporary remnants are untouched.

## Retirement condition

The user explicitly reaffirmed: retain the old implementation until the new IR
path is fully implemented and equivalent over the required supported behavior.
Extraction, preservation checks, narrow tests, and partial IR coverage are not
retirement evidence. This merge does not authorize deleting the old path or
weakening any conformance or architecture gate.

## Merge work

Conflicts affect carrier-bag-delete, declarations, calls-closures, calls,
generators-native, and index. Preserve both branches' changes; do not substitute
one entire file for the other. The class retained-delete path keeps locals3–5;
upstream anonymous-expando deletion appends its locals after that allocation.
The existing class helper, authenticated marker protocol, anonymous physical-key
collision decline, and existing finalization order remain intact.

An additive test exercises class deletion and two independent same-shaped
anonymous bags in one compiled module at O0 and O2. Original tests and known
failure fixtures remain unchanged. Test results and readiness are not yet
asserted by this initial continuation record.

## Remaining work

Current local integration evidence: all textual conflict markers are resolved.
The first TypeScript 7 check failed on two automatically merged `lowerForOf`
definitions; both implementations are now retained under distinct names with
pre-planning target dispatch, and the second TypeScript 7 check passed.
This routing remains subject to semantic review and focused execution tests.
Follow-up review found source-level admission differences: sibling loops sharing
a binding spelling and loops inside yielding finally blocks are rejected by the
state-region planner but admitted by upstream's delegation planner. These are
not yet runtime comparisons. Lane-only dispatch is provisional and must not be
published as a preservation-safe resolution. Compare those cases on upstream
and candidate and select using non-mutating admission checks before planning.
The stricter for-of iterator check retains upstream's common-property scan;
yield-star retains the existing oracle policy, avoiding accidental union widening.
Eight focused test files completed: 162/162 checks passed in132.21s, including
explicit known-defect preservation checks that are NOT conformance successes.
Log: `.tmp/focused-merge-20260922.log`.

New additive generator fixtures confirmed that lane-only dispatch regresses
standalone sibling loops and normal loops inside finally: both pass on exact
main6190 and refuse compilation on the provisional candidate. Both WASI
controls pass. Initial pending-return cases hit TypeScript void-return
diagnostics in both arms; original logs are retained. The identical sources
are additionally compared with semantic diagnostics disabled in both arms
(checker inference remains active) to measure JavaScript runtime semantics.
Candidate runtime comparison:3/6 pass,3/6 standalone compile refusals; exact
main6190 runtime comparison:6/6 pass, including pending return through finally.
Both arms use identical fixture source/options and the same Node environment.
Logs: `.tmp/generator-routing-candidate-runtime-20260922.log` and
`.tmp/generator-routing-main-6190-runtime-20260922.log`.
Do not publish the provisional dispatch.

The provisional lane-only dispatch has now been replaced by a pure
`stateRegionAdmitsForOf` check containing the original state-region early
guards. Eligible shapes retain that planner; other shapes enter upstream's
delegation planner with its original guards intact. No failed planner is retried.
All13 generator checks pass after this repair (six paired regression fixtures,
six original suspended-loop checks across both targets, one delegation control).

Four additional fixtures across both targets produce exactly matching main
and candidate outcomes (8/8 preservation, only2/8 correct JavaScript results):
captured iteration bindings return34 correctly; nested same-name bindings
return377477 instead of373474; mixed-type sibling bindings throw illegal cast
instead of131429; an unrelated nested function is refused instead of returning34.
These are retained failures, not successes or permission to retire code.
Runnable diagnostic fixture:
`plan/agent-context/5753-generator-routing-probes-20260922.mjs`.
Paired logs: `.tmp/generator-routing-probes-{candidate,main}-node-20260922.jsonl`.

The exact-main oracle gate found one remaining additional raw-checker site.
The strict iterator membership query now lives behind
`commonIteratorMembersOf`, preserving upstream common-property semantics.
Its tri-state result is consumed only when exactly true; the separate
existential union oracle used by yield-star is unchanged. Added six type-shape
expectations, including a partial iterator union and unknown/any.
Post-oracle TypeScript7 check passes; all18 targeted oracle/generator checks
pass in22.67s. The oracle ratchet against exact main6190 passes with
getTypeAtLocation+0 and ctx.checker-1, without adding an allowance.
The in-house backend explicitly reports unknown for the new query; differential
mode retains checker answers and records disagreement. In-house end-to-end
equivalence is not asserted. Paired in-house diagnostics initially caught six
changed outcomes, including two correct capture cases newly refusing.
An explicit one-query compatibility adapter now preserves that legacy
checker-owned decision for selected in-house mode. Pure InHouseOracle remains
unknown; differential mode compares against that pure implementation so the
missing capability remains visible. Repaired in-house outcomes match exact
main8/8, including the original failures. This adapter must remain until
replacement implementation and equivalence are proved.
Final compatibility revision: TypeScript7 passes and all18 targeted checks pass
in22.52s, including backend-factory parity and differential disagreement visibility.
Normal commit hooks will use exact main6190 as the change base while the merge is
uncommitted; the pre-merge HEAD merge-base otherwise includes already-landed main
changes. No gate allowance or baseline is changed.

Expanded the existing three suspended-for-of fixtures to both standalone and
WASI without changing their source or expectations. All6 pass, plus the
composed delegation/finally identity control:7/7 in16.18s.
Log: `.tmp/generator-both-lanes-20260922.log`.

Independent review of PR5883 identified three conflicted files and overlapping
Promise combinator implementations on current main. Its published-head CI run
35468016852, job105963794331 failed the stack-balance gate with
`default-value-lossy` increasing from43 to48. Downstream skipped steps are not
passing evidence. Diagnose the five added sites against the paired base; do not
raise the allowance or blindly choose either implementation.

Complete review, typecheck, focused source tests, and normal commit/push gates
before publishing to existing PR5753. Keep its hold until original full-population
regressions are resolved and protected-queue checks justify landing. PR5883 also
conflicts with current main and is under independent read-only blocker review.
No current-main delivery or complete end-to-end IR equivalence is claimed.
