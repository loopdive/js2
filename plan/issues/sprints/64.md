---
sprint: 64
status: active
---

# Sprint 64 — standalone conformance residuals (autonomous drive)

> Started 2026-06-19. Stakeholder directive: **autonomous drive, conformance
> residuals first**. Devs grind the tractable standalone-conformance pool for
> fast pass-rate wins; the architecture epics carried from s63 are **deferred**
> until the residual pool drains, then we pivot (architect-led) to them.

## Focus: conformance residuals first

Front-load the dev-claimable standalone-conformance fixes. Architecture epics
(async, Proxy, IR-first refactors) wait — they need architect `## Implementation
Plan`s and don't yield near-term pass-rate.

## Dispatch pool (dev-claimable, tractable)

Clean next-slice work (claim-first via `scripts/claim-issue.mjs`, slice-or-triage):
- **#2160** standalone String/Number coercion residual
- **#2161** standalone RegExp engine residual
- **#2162** standalone Map/Set/Weak collections residual
- **#2158** standalone class-prototype descriptor residual
- **#2159** standalone TypedArray/DataView buffer residual
- **#2029** standalone u32 out-of-range binary emit
- **#1538** Wasm-native JSON.parse/stringify (Ryū #1537 landed → unblocked)
- **#1938** linear number[] i32 truncation / double-eval
- **#2045** linear Uint8Array soundness holes
- **#2001** sparse holes materialize defaults
- **#1344** spec-gap generator-prototype receiver checks

Fresh conformance slices (2026-06-19 PO refill — standalone failure mining;
genuinely independent of the in-progress epics, ranked by pass-rate impact):
- **#2200** Annex B B.3.3 block-level function hoisting (~186; host-agnostic) — **highest impact**
- **#2203** array destructuring elision + default miscount (~54 standalone CE + host fails)
- **#2202** `arguments.length` wrong for trailing-comma+spread in generator methods (~30)
- **#2201** logical-assignment NamedEvaluation `x ??=/||=/&&= fn` → `fn.name` (~9; XS)

In-flight (resolve + land):
- **#6408** standalone objlit data/method key sentinel — PR #1710 (conflict)
- **#2051** optional-chain undefined repr — PR #1708 (CLEAN, enqueue)
- **#1917** single coercion engine slice — PR #1709 (CLEAN, enqueue)
- **#2026** class-as-first-class-value — PRs #1711/#1704 (regression-failing, rework)

Max-effort cores (senior-dev when residual pool thins):
- **#2009** same-shape struct field-name collision
- **#2106** valuerep P3 undefined observability
- **#2151** standalone any-receiver method dispatch

## Deferred to later in s64 (architecture-led, after residual pool drains)

- **Async epic**: #1042 (state machine), #1373b (IR CPS), promise-async-capability-residual
- **Proxy epic**: #1355 (pure-Wasm Proxy traps)
- **Self-hosting**: #1712 (acorn differential)
- **IR-first refactors**: #1916, #1926, #1927, #1919, #1930, #2138, #2134, #2135
- **Funcidx/pipeline arch**: #1899, #2087, #2146, #742, #2083
- **Blocked** (deps unmet): #1985, #2013, #2044, #2140, #2141, #2167, #2173, #2181

## Notes
- Carried wholesale from s63's honest carry-and-close (74 done, 34 carried).
- Shepherd Monitor + GitHub auto-enqueue backstop drive the merge queue.
