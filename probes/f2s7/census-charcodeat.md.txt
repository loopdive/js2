# census: charcodeat
grounding: a07f65319f1d31b4ec8e528651c5afbb6d66a320 (origin/main, measured in scratch worktree census-charcodeat, now removed). The read-only checkout /home/user/js2 is at 9e4fc20aff, which differs from origin/main only in tests/issue-5272-runner-standalone-leak.test.ts and tests/test262-runner.ts (git diff --stat verified) — no seam file differs, so every line number below holds on both.
## verdict
- verifiedSites: 40  confidence: high — 65/65 matrix cells reproduced byte-for-byte (ok/bytes/sha) on a fresh worktree at the census commit, 4 fixtures × 7 lanes re-instrumented at every cited arm with reach counts matching, the pre-existing red reproduced, and every cited file:line was opened; the wrong items are line drift, one wrong reachability claim, one wrong test-inventory claim, and presentation of the imports column, not the seam structure.
### wrongSites
- GROUNDING: '/home/user/js2 at 9e4fc20aff differs from origin/main only in tests/issue-5272-runner-standalone-leak.test.ts and tests/test262-runner.ts' — false. `git diff --stat origin/main HEAD` = 20 files, including 9 under src/codegen/ (builtin-value-read.ts, closed-method-dispatch.ts, dataview-native.ts, declarations.ts, expressions/call-namespace-static.ts, expressions/new-builtin-globals.ts, expressions/new-indexed.ts, property-access-dispatch.ts, property-access.ts) plus tests/issue-5150-es2015-buffers.test.ts, plan/ and benchmark JSON. HEAD is an ancestor of origin/main (18 commits behind). No cited SEAM file differs, so the cited line numbers do hold on both trees — but the matrix bytes were re-measured on a07f65319f, not on HEAD.
- src/codegen/char-code-at-helpers.ts:332 'export function ensureNativeFlatCharCodeAtHelper(ctx)' — line 332 is a doc-comment line; the function is at :342. Signature claim ((ref null $AnyString,i32)->i32, no flatten) holds at :351-356.
- src/codegen/char-code-at-helpers.ts:371 'export function ensureHostCharCodeAtTrusted(ctx) {' and the range ':371-386' — the function is at :380 (body :380-390); :371 is blank. ONE-record claim (only `jsStringImports.get("charCodeAt")`, :381) and (externref,i32)->i32 (:386-387) hold.
- src/ir/from-ast.ts:8784 'cx.builder.emitCall(irIntrinsicFuncRef(funcName), loweredArgs, callResultType)' — :8784 is `continue;` inside the NaN-pad branch; the emitCall is at :8792.
- src/ir/from-ast.ts:8672 'const plan = cx.resolver?.stringMethodPlan?.(...)' — that line is :8673 (:8672 is the preceding comment). One-off, content correct.
- laneReads/answers: 'strictNoHostImports auto-enables nativeStrings per src/index.ts:660' — :660 is a doc-comment about `unionAnyRep`. The derivation lives in src/target-profile.ts:124-125 (`nativeStringsRequiredByPolicy: target==="standalone"||target==="wasi"||strictEnvImportGate||…`) consumed by src/codegen/context/create-context.ts:81 (`options?.nativeStrings ?? !!(options?.fast || targetProfile.nativeStringsRequiredByPolicy || options?.utf8Storage)`).
- laneReads/risks: 'the `standalone||wasi||strictNoHostImports` guard is unreachable from compile() options' — false. The `??` at create-context.ts:81 keeps an EXPLICIT `nativeStrings: false`, so `compile(src, {strictNoHostImports:true, nativeStrings:false})` and `{target:"wasi", nativeStrings:false}` reach `charReadPlan/host-decline` (measured 1× each on LOOP and LOOPNC) and `stringMethodPlan native=false` + arm :6417 (READ). The compile then fails on `Host import "string_constants.<fn>" requested under --no-host-imports / WASI strict mode`, so no module is emitted — but the arm is reachable, contrary to '0 reaches in 71 cells'. The census probe evidently omitted nativeStrings rather than passing false.
- pins/answers(5): 'no test names ensureHostCharCodeAtGuarded/ensureNativeCharCodeAtHelper/IR_STRING_CHAR_CODE_AT_FN in tests/' — `IR_STRING_CHAR_CODE_AT_FN` is imported and asserted in tests/issue-3521-prepared-component-dependencies.test.ts:50 and :1019 (pins that the prepared `string.char_code_at` instr carries `provider: {binding:{kind:"intrinsic", symbol: IR_STRING_CHAR_CODE_AT_FN}}`). The narrower claim — no test pins the :6527 arm's SOURCE TEXT / the two ensure* names — does hold.
- pins: 'the whole-shape policy equality in tests/issue-3526-ir-runtime-manifest.test.ts' MOVES — could not confirm: that file has no `.policy` assertion, no `stringLen`/`stringEq`/`stringCompare`/`numberBoundary` mention, and no `toEqual({ target` literal. Only the {compare,eq,len} suites (compare:268, eq:301, len:350) carry `frozen.policy.stringLen` equality pins.
- matrix 'imports' column: it is a filtered, RENUMBERED view, not the module's import section. gc-host cells also import `string_constants.<fn>#2`, `string_constants.#3` (and OOB `string_constants.abc#3`, TPL `env.__concat_3#5`, FOROF env.__box_number/__iterator…); CHAIN's `env.string_trim` is #4 not #2, SUBCONST's `env.string_substring` is #4 not #2; every gc-native-strings cell imports `env.__str_from_mem#0 env.__str_to_mem#1 env.__str_extern_len#2`, not '-'. The narrower statement 'native lanes import nothing FOR THE SEAM' is true.
- consumers: ':6527 resolveFunc arm … (5 cells: 1 host, 4 native incl. probes)' and 'emitStringCharCodeAt adapter (provider branch 5/65)' — within the 65-cell matrix it is 4/65 (OOB × gc-host, gc-native-strings, standalone, wasi); OOB linear reaches neither :6527 nor the GC adapter (measured: linear OOB hits no integration.ts arm). The 5th is the out-of-matrix strict probe. The laneReads text itself says 'OOB on every lane except linear', which is 4.
### numberChecks
- 65-cell matrix re-run on a fresh detached worktree at a07f65319f (options: {} / {nativeStrings:true} / {target:'standalone'} / {target:'wasi'} / {target:'linear'}, fileName 'f2-cca.ts'): ok flag, byte count and sha256[0:12] → 65/65 identical to the census for every cell (e.g. LOOP gc-host 313 d05f4670b197; LOOP native 22601 cbda921b0e74; LOOPNC gc-host 377 c9c4c55f80b5; OOB native 22191 414ad53b55ba; READ linear 5621 5aed38e456dd; CLEAN linear 4878 eab66857b0be; all 7 linear failures bytes 0 / e3b0c44298fc). emitWat:true does not change the binary (65/65 same sha).
- WAT symbol reach (all 65 cells, emitWat): LOOP/LOOPSUM gc-host → `__jsstr_charCodeAt_trusted` ✓; LOOP/LOOPSUM × native/standalone/wasi → `__str_flat_charCodeAt` + `__str_flatten` + `$$slot___cca_flat` ✓; LOOPNC/READ/CONST/NEG/OMIT/TPL/CHAIN gc-host → `__jsstr_charCodeAt` ✓, same 7 + FOROF on the 3 native lanes → `__str_charCodeAt` ✓; READ/CONST/NEG/OMIT/OOB linear → `__linear_ir_str_char_code_at` ✓; SUBCONST gc × 4 → no charCodeAt helper (legacy arm) ✓; FOROF gc-host → none ✓.
- Instrumented reach counts (console.error at :5906, :6004/:6005/:6011/:6012, :6417, :6421, :6432, :6437, :6526, :6974, :6978; reverted after): LOOP gc-host {charReadPlan/host-trusted:1, 6437:1}; LOOP native+standalone {charReadPlan/native-hoist:1, 6425 __str_flatten:1, 6425 __str_flat_charCodeAt:1}; LOOPNC gc-host {charReadPlan/host-trusted:1, stringMethodPlan native=false:1, 6417:1} (confirms charReadPlan runs BEFORE shape detection on a non-canonical loop); LOOPNC native {charReadPlan/native-hoist:1, stringMethodPlan native=true:1, 6421:1}; READ gc-host {plan:1, 6417:1}, READ native {plan:1, 6421:1}; OOB gc-host {6527 nativeStrings=false:1, 6974/provider:1}; OOB native/standalone {6527 nativeStrings=true:1, 6974/provider:1}; OOB linear {} ; :6978 adapter fallback 0 in every probed cell. All match the census 'reaches' strings.
- Path totals recomputed from WAT: plan-path 35 (7 gc-host + 8×3 native lanes = 24 + 4 linear) ✓; instr path 5 (OOB×5, but only 4 pass through :6527) ✓/see correction; hoist/trusted 8 (2 trusted + 6 hoist) ✓; demote/fail 12 (SUBCONST×4 gc + FOROF gc-host + 7 linear) ✓; CLEAN 5 ✓; sum 65 ✓.
- Demote messages: FOROF gc-host = `ir/from-ast: method call .charCodeAt(...) on externref not in slice 4 (forof) [IR-FALLBACK]` ✓; SUBCONST gc lanes = `method call .charCodeAt(...) on string not in slice 4 (sub)` ✓; OMIT every lane reports TS diagnostic `Expected 1 arguments, but got 0.` with success=true ✓ (census noted it only for linear; it is present on all 5 lanes).
- Pre-existing red: `vitest run tests/string-derived-length-fast-path.test.ts` on clean a07f65319f → 1 failure, 'applies immutable derived-result proofs to host strings without crossing the JS boundary' → `illegal cast` ✓; 'reuses the flat descriptor of a const substring for charCodeAt' (the :344 pin) passes ✓.
- STILL_UNPROVIDED_IDS: NEW_IDS (6, :75-82) minus PROVIDED_IDS ['string.eq','string.len'] (:93) = 4; `expect(STILL_UNPROVIDED_IDS).toHaveLength(4)` at :378 ✓.
- IntrinsicSignature constants in src/ir/intrinsics.ts:250-307 = 8 (F64_TO_EXTERNREF, EXTERNREF_TO_F64, I32_TO_EXTERNREF, EXTERNREF_TO_I32, EXTERNREF_PAIR_TO_I32, F64_TO_U32, F64_UNARY, F64_BINARY); none has params [EXTERNREF, I32] ✓. EXTERNREF_PAIR_TO_REF_EXTERN is not in the tree (F2-S5 unlanded) — unverifiable, stated as future.
- RuntimeProviderImplementation union (runtime-manifest.ts:450-521) = 10 kinds: backend-op, backend-sequence, backend-composite, self-hosted, host-capability, host-callable, runtime-callable, carrier-field, host-managed, native-managed — no pair-shaped kind ✓. Validation :1716-1733 forbids hostCapabilities only on host-managed / native-managed and requires ≥1 on host-capability; :1738-1745 host-callable id narrowing; :1754-1760 carrier-field forbids capabilities — nothing forbids capabilities on runtime-callable ✓. :565-570 host-capability/host-managed → async host family, throws on mix ✓. #selectProvider at :1793 dispatches per-feature policy selectors (number/boolean/extern… chain) ✓. No `charCodeAt`/`char_code_at` string anywhere in runtime-manifest.ts (nothing landed) ✓. StringLenPolicy sibling shape :240-252, policy fields :254-303, canonicalization :1472/:1481 ✓; partition template at :3880-3891 refuses `len === "unsupported"` with `late-preparation-unsupported` ✓.
- runtime-host-capabilities.ts:286 `funcRecord("string.char_code_at","wasm:js-string","charCodeAt",["externref","i32"],["i32"])` ✓ and :304 `string.len` record ✓ (the two records the guarded host helper closes over: char-code-at-helpers.ts:177-179 reads both `charCodeAt` and `length` imports, sig (externref,i32)->f64 at :181 ✓; native helper sig (ref $AnyString,i32)->f64 at :239 ✓).
- from-ast.ts reads no lane flag: 29 textual `nativeStrings` hits are all comments except one error-message string at :8761 ✓; char-read-loop.ts: 3 hits, all comments ✓.
### corrections
- Fix grounding text: HEAD 9e4fc20aff is 18 commits BEHIND origin/main a07f65319f and differs in 20 files (9 under src/codegen/), none of them seam files; say 'no cited seam file differs' rather than 'only two test files differ'.
- Replace 'src/index.ts:660' with 'src/target-profile.ts:124-125 (`nativeStringsRequiredByPolicy`) consumed by src/codegen/context/create-context.ts:81'.
- Drop the 'charReadPlan host-decline guard is unreachable from compile()' claim (and the matching risk/unmeasured entries): explicit `nativeStrings:false` under strictNoHostImports/wasi reaches host-decline, stringMethodPlan native=false and arm :6417; the compile then fails on `string_constants.*` host imports under strict mode. Keep 'never emits a module' if that is the point.
- Correct line cites: char-code-at-helpers.ts :332→:342 (ensureNativeFlatCharCodeAtHelper), :371→:380 (ensureHostCharCodeAtTrusted, body :380-390); from-ast.ts :8784→:8792 (plan-path emitCall), :8672→:8673.
- Correct counts: :6527 arm and GC adapter provider branch are 4/65 (OOB × 4 GC lanes), not 5/65; the 5th was the strict probe outside the matrix. OOB linear reaches neither.
- Rewrite the matrix 'imports' column as 'seam-relevant imports (filtered)' or list the real import section — gc-native-strings cells carry env.__str_from_mem/__str_to_mem/__str_extern_len, gc-host cells carry string_constants.* and the ordinal indices for string_trim/string_substring are #4, not #2.
- Correct the pin inventory: IR_STRING_CHAR_CODE_AT_FN IS named in tests/issue-3521-prepared-component-dependencies.test.ts:50,1019 (provider-attach pin on the prepared instr); keep the narrower 'no test pins the :6527 arm source text or the two ensure* helpers'.
- Remove tests/issue-3526-ir-runtime-manifest.test.ts from the 'MOVES: whole-shape policy equality' list, or cite the actual assertion — none was found. The frozen-default pins that move are compare:268, eq:301, len:350 (`frozen.policy.stringLen`).
- OMIT: the 'Expected 1 arguments, but got 0.' diagnostic with success=true appears on ALL five lanes, not only linear.

## answers
(1) Providers serving charCodeAt today — 6 physical symbols + 1 semantic, 4 WasmGC resolve arms + 1 lane-reading instr arm:
 - `__jsstr_charCodeAt` (host GUARDED; a DEFINED helper closing over TWO capability records `string.char_code_at` + `string.len`, `(externref,i32)->f64`, char-code-at-helpers.ts:173-221). Selected by `ctx.nativeStrings === false` at plan time (:5907, symbol baked into the call) → arm :6417 [7 cells], and at resolve time (:6527) for the `string.char_code_at` instr [1 cell, OOB host].
 - `__str_charCodeAt` (native GUARDED; defined helper, flatten + array.get_u, `(ref $AnyString,i32)->f64`, :227-303). `ctx.nativeStrings === true`, same two sites → :6421 [24 cells] and :6527 [3 cells + 2 probes].
 - `__jsstr_charCodeAt_trusted` (host UNGUARDED; one record `string.char_code_at`, `(externref,i32)->i32`, :371-386). charReadPlan :6012 (`!nativeStrings` and not standalone/wasi/strict) + AST in-bounds proof → :6437 [2 cells: LOOP, LOOPSUM gc-host].
 - `__str_flatten` + `__str_flat_charCodeAt` (native HOIST pair; :332-360). charReadPlan :6003-6008 (`nativeStrings` + struct types registered) + proof → :6425 [6 cells, LOOP/LOOPSUM × 3 native lanes; each cell resolves both symbols once].
 - `__linear_ir_str_char_code_at` (linear runtime; no lane read, backend identity; linear-integration.ts:1560/1637) [4 plan-path cells + OOB instr cell; 7 linear cells fail in legacy-linear `Unsupported method call: .charCodeAt()`, pre-existing].
 - semantic `__ir_string_char_code_at` (IR_STRING_CHAR_CODE_AT_FN, string-runtime.ts:47) attached unconditionally to every `string.char_code_at` instr (string-support.ts:70/137), resolved at :6527.
 The lane fact is ONE flag everywhere: nativeStrings. charReadPlan's extra `standalone||wasi||strictNoHostImports` guard was reached 0 times in 65 cells + 6 probe cells (strictNoHostImports auto-enables nativeStrings, src/index.ts:660), so guarded/trusted/native/flat are: guarded-host ⇔ !nativeStrings & no proof; trusted ⇔ !nativeStrings & proof; guarded-native ⇔ nativeStrings & no proof; flat-hoist ⇔ nativeStrings & proof & struct types.
(2) Matrix: 13 fixtures × 5 lanes = 65 cells (table above; artefacts in scratchpad f2-cca-matrix-before.{md,json}, driver f2-cca-matrix.mts, instrumentation f2-cca-instrument.py, probe f2-cca-probe-strict.mts). Reach totals: plan path (stringMethodPlan → intrinsic call) 35 cells (7 host, 24 native, 4 linear); instr path (`string.char_code_at` → :6527 → adapter provider branch) 5 cells (OOB only — the one fixture with receiver-encoding evidence, a literal); hoist/trusted 8 cells (LOOP, LOOPSUM × 4 GC lanes); demote/fail 12 (SUBCONST×4 gc `method-call-unsupported` via preferLegacyFlatSubstring — reached 2× per cell, second reach unattributed; FOROF gc-host `charCodeAt on externref not in slice 4`; linear compile-fail ×7); no charCodeAt 5 (CLEAN). Adapter fallback (:6978) 0/65. Host import block: `wasm:js-string.length#0, charCodeAt#1` in every emitting gc-host cell; native lanes import nothing for the seam.
(3) Plan-time (from-ast build, via resolver callbacks that LIVE in integration.ts but run before any IR exists): stringMethodPlan :5898/:5907 (reads nativeStrings, bakes the lane into the SYMBOL NAME), charReadPlan :6002-6012 (reads 4 flags + struct-type presence; decides hoist/trusted/none), preferLegacyFlatSubstringCharCodeAt :5964 (checker only, no lane). Resolve-time lane reads: :6527 ONLY (and the dead :6978 fallback). Already resolver-owned decisions: all of the above are in integration.ts (from-ast reads no flag — #2955 discipline), but only :6527 decides at the point the F2-S3/F2-S4 arms decide; arms :6417/:6421/:6425/:6437 consume a plan-time decision and read nothing. So the R6-shaped move is :6527 + demand; the plan symbols are a from-ast-side vocabulary and stay out of scope — with the consequence that a manifest policy can only VERIFY (fail-closed) the plan's symbol at :6417/:6421, not re-decide it, unless a later slice neutralises the plan symbol to the semantic one.
(4) Manifest: ONE policy `StringCharCodeAtPolicy { charCodeAt: "host" | "native" | "unsupported" }` (one lane axis, measured). Features: slice 1 = ONE feature `js.string.char_code_at` (guarded read, semantic `(string,i32)->f64`) with two rows; the trusted/flat arms are a SECOND feature `js.string.char_code_at.trusted` (semantic `(string,i32)->i32`) whose native row is a symbol PAIR + preheader slot protocol decided by charReadPlan — recommend deferring it (plan-time seam) or adding it as rows the arms only verify. Implementation kind: the existing multi-record `host-capability` kind FITS the two-record shape syntactically (`{kind:"host-capability"}` + `hostCapabilities:["string.char_code_at","string.len"]`, validation :1728 requires ≥1) but is COUPLED to the async family projection (`projectRuntimeBackendRequirements` :565 treats `host-capability` as the host-async family and throws on mixing with `native-managed`) — do not reuse it. `host-callable` does not fit either: the host provider is a DEFINED helper (`ensureHostCharCodeAtGuarded`), not the import itself, so the F2-S3 `exactCallableImportIndex(ctx, module, field)` arm template does not transfer. Best fit without a new kind: `{kind:"runtime-callable", symbol:"__jsstr_charCodeAt"}` with `hostCapabilities:["string.char_code_at","string.len"]` (no validation rule forbids capabilities on runtime-callable; only host-managed/native-managed/carrier-field are restricted, :1716-1757) and native `{kind:"runtime-callable", symbol:"__str_charCodeAt"}`, `[]`. If the lane wants the row to say 'defined helper over records' explicitly, a new kind `composed-callable { symbol, capabilities }` is the honest one. Signatures: NO existing IntrinsicSignature has `[externref,i32]` params (existing: F64_TO_EXTERNREF, EXTERNREF_TO_F64, I32_TO_EXTERNREF, EXTERNREF_TO_I32, EXTERNREF_PAIR_TO_I32, F64_TO_U32, F64_UNARY, F64_BINARY; F2-S5 adds EXTERNREF_PAIR_TO_REF_EXTERN) — mint `EXTERNREF_I32_TO_F64_INTRINSIC_SIGNATURE` (guarded) and, for the trusted feature, `EXTERNREF_I32_TO_I32_INTRINSIC_SIGNATURE` (= the capability record's ABI). Demand scan must count BOTH producers: `string.char_code_at` instrs AND intrinsic `call`s to JSSTR_CHARCODEAT_FN/NATIVE_CHARCODEAT_FN (the :7268 preregistration scan already enumerates the host one) — an instr-only scan would freeze a row for 5/65 cells and miss 35.
(5) Pins: tests/issue-3931.test.ts:101-125,158-268,270-305,309-410 (shape + runtime, all 5 configs; behaviour — stay); tests/issue-1105-charcodeat.test.ts:38-110 (native NaN/ToInteger/i32-pure; stay); tests/issue-3526-string-boundary-schema.test.ts:76,94,204-211,313,378 (record row, ABI vs registration, STILL_UNPROVIDED fence — MOVES: `toHaveLength(4)`→3 after F2-S5, →2 for charCodeAt, PROVIDED_IDS gains `string.char_code_at`); tests/issue-3520-callable-preregistration.test.ts:13-27 (user function named `__jsstr_charCodeAt` must not resolve as the intrinsic — stays, and pins that the new row's symbol is binding-aware); tests/string-derived-length-fast-path.test.ts:344 (const-substring receiver → NO `__str_charCodeAt` helper; stays, legacy path); tests/linear-charcodeat-ascii-fast-path.test.ts (linear runtime; stays under DISABLED policy); tests/issue-3521-prepared-component-dependencies.test.ts:994 (instr kind listed; stays). Frozen-policy default pins that MOVE (gain `stringCharCodeAt: { charCodeAt: "unsupported" }`): tests/issue-3526-string-boundary-{compare,eq,len}.test.ts defaults blocks and the whole-shape policy equality in tests/issue-3526-ir-runtime-manifest.test.ts. NO existing test pins the :6527 arm's source text (no test names ensureHostCharCodeAtGuarded/ensureNativeCharCodeAtHelper/IR_STRING_CHAR_CODE_AT_FN) — there is no "un-migrated charCodeAt arm still reads the lane" fence today, unlike concat (schema :641-655). Pre-existing RED on the grounding tree: tests/string-derived-length-fast-path.test.ts "applies immutable derived-result proofs to host strings without crossing the JS boundary" fails with `RuntimeError: illegal cast` on clean origin/main a07f65319f (re-run after reverting instrumentation; f2-cca-pin-clean.out) — not this seam's, but it is in a suite this slice would touch.

## laneReads
- src/ir/integration.ts:5898+5907 — stringMethodPlan reads ctx.nativeStrings at PLAN time (from-ast build) and bakes the lane into the call SYMBOL (`__str_charCodeAt` vs `__jsstr_charCodeAt`); decides the guarded helper for every method-plan call (35/65 cells reach it: 7 gc-host, 24 native-strings lanes, 4 linear via the linear twin)
- src/ir/integration.ts:6002-6012 — charReadPlan reads ctx.nativeStrings, ctx.anyStrTypeIdx/nativeStrTypeIdx/nativeStrDataTypeIdx, ctx.standalone, ctx.wasi, ctx.strictNoHostImports at PLAN time; decides hoist-pair (native) vs trusted (host) vs decline; called once per for-statement BEFORE shape detection (LOOPNC reaches it too). Measured: native-hoist 12 cells, host-trusted 3 cells, native-decline 0, host-decline 0 in 65 cells + 6 probe cells (strictNoHostImports auto-enables nativeStrings per src/index.ts:660, so the `standalone||wasi||strictNoHostImports` guard is unreachable from compile() options)
- src/ir/integration.ts:6527 — the `IR_STRING_CHAR_CODE_AT_FN` resolve arm reads ctx.nativeStrings at RESOLVE time; decides guarded host vs guarded native for the `string.char_code_at` INSTR path. Reached in 5/65 cells (OOB on every lane except linear — the only fixture with receiver-encoding evidence, a literal receiver). THIS is the R6-shaped decision.
- src/ir/integration.ts:6978 — emitStringCharCodeAt adapter fallback reads ctx.nativeStrings when no provider is attached; reached 0/65 (provider branch 5/65)
- src/ir/integration.ts:6417/6421/6425/6437 — resolve arms read NO lane flag: the lane is already encoded in the symbol chosen at plan time; they only materialize (consumer)
- src/ir/backend/linear-integration.ts:1560/1637 — no lane read; backend identity is the choice (linear plan 8 cells called, 4 succeeded + OOB instr path 1)

## manifestNeeds
Schema: (a) `StringCharCodeAtPolicy { readonly charCodeAt: "host" | "native" | "unsupported" }` + `STRING_CHAR_CODE_AT_POLICY_DISABLED`, `RuntimeManifestPolicy.stringCharCodeAt?`, frozen twin, canonicalization (siblings of stringLen at runtime-manifest.ts:240-302, :1472-1481). (b) Feature `js.string.char_code_at` (guarded semantic read) with rows `host.js.string.char_code_at` and `native.js.string.char_code_at`; optional second feature `js.string.char_code_at.trusted` for the proof-licensed arms (host row `__jsstr_charCodeAt_trusted` over `["string.char_code_at"]`; native row is a symbol PAIR `__str_flatten`+`__str_flat_charCodeAt` with a preheader-slot protocol — no existing implementation kind expresses a pair, and the decision is plan-time, so defer). (c) Implementation kinds: reuse `runtime-callable` for BOTH rows, with the host row carrying `hostCapabilities: ["string.char_code_at","string.len"]` — legal today (only host-managed/native-managed/carrier-field forbid capabilities, :1716-1757); do NOT reuse `host-capability` (bound to the async family projection at :565) and `host-callable` does not fit (the host provider is a defined helper that CLOSES OVER two import records, not the import; the arm materialises via `ensureHostCharCodeAtGuarded`, which reads `ctx.jsStringImports`, so `exactCallableImportIndex` is the wrong routine). If the lane wants the manifest to state 'defined helper over N records' as a first-class fact, add kind `composed-callable { symbol; capabilities }`. (d) Two new IntrinsicSignature constants: `EXTERNREF_I32_TO_F64_INTRINSIC_SIGNATURE` (params [EXTERNREF, I32], result F64 — the guarded helpers' semantic shape; the native helper physically takes `ref $AnyString`, reused nominally like len/eq) and `EXTERNREF_I32_TO_I32_INTRINSIC_SIGNATURE` (the record's ABI, for the trusted feature). (e) Demand: `stringCharCodeAtDemand` computed from `string.char_code_at` instrs OR intrinsic calls to `__jsstr_charCodeAt`/`__str_charCodeAt` (mirror :7268's enumeration); both producers must be scanned or 35/65 cells freeze no row. (f) `preparedStringCharCodeAtProvider(prepared)` → `{arm:"host", symbol, capabilities:[…]} | {arm:"native", symbol}`; resolve arm :6527 swaps to it (fail-closed `selection-preparation-mismatch`); arms :6417/:6421 keep their materializers but assert the plan symbol matches the frozen row (mismatch = invariant, the only way policy can disagree with the plan-time read). (g) Partition block near :3782-3905 refusing `charCodeAt === "unsupported"` with demand → `late-preparation-unsupported` naming `string-char-code-at policy`. (h) Adapters: linear-integration.ts:683 and stdlib-selfhost.ts:510 gain `stringCharCodeAt: STRING_CHAR_CODE_AT_POLICY_DISABLED`; neither passes demand, linear lowers through `__linear_ir_str_char_code_at` regardless (measured). (i) Host pre-registration ordering: the guarded/trusted helpers read `ctx.jsStringImports` at materialization — `addStringImports` must have run before freeze consumers resolve; :7268 already forces it for the plan symbols, the instr path is covered by `instrUsesStrings` :7435.

## sliceScope
IN (one slice, byte-neutral by construction — 65/65 sha must match f2-cca-matrix-before.json): policy `stringCharCodeAt` + feature `js.string.char_code_at` + two `runtime-callable` rows (host row over records `string.char_code_at`,`string.len`) + `EXTERNREF_I32_TO_F64_INTRINSIC_SIGNATURE`; demand scan over BOTH producers (instr + plan-symbol calls); `preparedStringCharCodeAtProvider`; resolve arm :6527 loses `ctx.nativeStrings`; arms :6417/:6421 verify the plan symbol against the frozen row (fail-closed) but keep `ensureHostCharCodeAtGuarded`/`ensureNativeCharCodeAtHelper`; adapter :6974 fallback retired fail-closed (0/65 reaches); partition block; DISABLED in linear + stdlib-selfhost adapters; schema-suite fence shrink; new tests/issue-3526-string-boundary-charcodeat.test.ts (contract / policy / end-to-end incl. runtime oracle on NaN-OOB, negative, omitted, fractional index, surrogate halves; sha fence on LOOP/LOOPSUM cells so the hoist arms are proven untouched). OUT: stringMethodPlan :5898-5907 and charReadPlan :6002-6012 lane reads (plan-time; R6 excludes from-ast-side decisions — a later slice can neutralise the plan symbol to `IR_STRING_CHAR_CODE_AT_FN` so the arm re-decides); preferLegacyFlatSubstringCharCodeAt :5964 and the SUBCONST demote; the hoist pair :6425 and trusted :6437 arms and a `.trusted` feature (plan-time seam, symbol pair); the 7 linear `Unsupported method call: .charCodeAt()` compile failures and the FOROF gc-host `charCodeAt on externref` demote (pre-existing, unrelated); char-code-at-helpers.ts bodies; the `string.len` record's own governance (F2-S4, landed).

## pins
- tests/issue-3526-string-boundary-schema.test.ts:94/:378 STILL_UNPROVIDED_IDS fence — MOVES (shrinks by `string.char_code_at`, PROVIDED_IDS grows; asserts positively that a provider names it)
- tests/issue-3526-string-boundary-schema.test.ts:204-211 whole-shape record row + :313 ABI-vs-registration — stay (record unchanged)
- tests/issue-3526-string-boundary-{compare,eq,len}.test.ts frozen-policy default blocks + tests/issue-3526-ir-runtime-manifest.test.ts whole-shape policy equality — MOVE (gain `stringCharCodeAt: { charCodeAt: "unsupported" }`)
- tests/issue-3931.test.ts:101-125 (shape: `$$slot___cca_flat`, `array.get_u`, no `f64.const nan`, no `i64.`), :158-268 hard receivers, :270-305 standalone/wasi, :309-410 negatives — stay (behaviour pins on hoist/trusted arms, which the slice does not move)
- tests/issue-1105-charcodeat.test.ts:38-110 — stay (native guarded semantics)
- tests/issue-3520-callable-preregistration.test.ts:13-27 — stay; additionally pins that a manifest row spelled `__jsstr_charCodeAt` must stay binding-aware (user export of that name compiles as a source unit)
- tests/string-derived-length-fast-path.test.ts:344 — stay (const-substring receiver demotes to legacy via preferLegacyFlatSubstringCharCodeAt; `(func $__str_charCodeAt` absent); NOTE same file's 'applies immutable derived-result proofs to host strings' is RED on clean origin/main a07f65319f (`RuntimeError: illegal cast`), pre-existing
- tests/linear-charcodeat-ascii-fast-path.test.ts:142+ — stay (linear ignores the policy, DISABLED)
- tests/issue-3521-prepared-component-dependencies.test.ts:994 — stay
- No test pins the :6527 arm's source text today (grep: no ensureHostCharCodeAtGuarded / ensureNativeCharCodeAtHelper / IR_STRING_CHAR_CODE_AT_FN in tests/) — a new suite should add the 'contains preparedStringCharCodeAtProvider(, no nativeStrings' pin, and the schema suite currently has NO 'un-migrated charCodeAt arm still reads the lane' fence to invert

## risks
- Double decision: the same flag is read at plan time (:5907) and would be read from the frozen row at resolve; a policy value that disagrees with the plan (only reachable via adapters that pass a policy AND a demand) can only be refused, not re-decided — the slice must make :6417/:6421 assert symbol==row and document that `unsupported` partitions the owner, it does not re-lower.
- Demand under-count: an instr-only scan (F2-S3/F2-S4 template) freezes a row for only 5/65 cells; the 35 plan-path cells carry an intrinsic `call`, not a `string.char_code_at` instr. The scan must enumerate both or the resolve arm :6417/:6421 verification has no row to verify against.
- `host-capability` kind looks like the fit ('two-record provider', F2-S3 plan wording) but is wired to the async family projection (:565 throws on host/native mix, :1728 ≥1 record) — reusing it for a sync string row risks tripping `invalid-backend-requirement-projection` in async modules; the census did not test that interaction.
- The host arm is a DEFINED helper over two import records, so the F2-S3 arm template (`exactCallableImportIndex(ctx, arm.module, arm.field)`) is wrong here; a slice that copies it verbatim would call the raw builtin and trap out of range (#2003).
- Unconditional semantic attach (string-support.ts:70/:137): fine for this seam because :6527 re-decides, but the F2-S4 lesson (omnibus pass re-derives six seams) applies if a dedicated attach is attempted.
- `preferLegacyFlatSubstringCharCodeAt` reached 2× per SUBCONST cell; the second reach is unattributed (likely a second lowering attempt) — harmless to bytes, but a demand scan run per attempt would see it twice.
- Pre-existing red: tests/string-derived-length-fast-path.test.ts 'applies immutable derived-result proofs to host strings…' fails on clean origin/main (`illegal cast`) — a PR touching this seam will run that suite and must not be blamed for it; report it upstream.
- Linear lane: 7/13 fixtures fail to compile at all (legacy-linear `.charCodeAt()` unsupported) — the linear DISABLED policy pin can only be proven on READ/CONST/NEG/OMIT/OOB.
- charReadPlan's `standalone||wasi||strictNoHostImports` guard is dead from compile() (0 reaches in 71 cells) but may be reachable from hand-built ctx callers (generateModule with a custom ctx) — not measured.

## unmeasured
- Runtime results were not executed for the 65 cells (no instantiate/oracle run); the census is byte/sha/reach only. Existing suites (3931, 1105) cover runtime semantics for the guarded/hoist arms and passed on the instrumented tree.
- The `fast: true` lane (tests/issue-3931 config 'fast+nativeStrings') was not in the 5-lane matrix.
- Why preferLegacyFlatSubstringCharCodeAt fires twice per SUBCONST cell — not attributed (no per-attempt instrumentation).
- Whether a sync `host-capability`-kind row would trip `projectRuntimeBackendRequirements` in a module that also freezes async rows — not probed.
- charReadPlan `host-decline` arm reachability from non-compile() entry points (hand-built CodegenContext) — only compile() options were probed (strictNoHostImports with and without nativeStrings; both resolved native).
- test262 delta of a future slice — zero by design (65/65 sha fence), not run.
- Byte-identity of standalone/wasi cells against a second run — single run per cell; the F2-S5 census established determinism for the same driver but this census did not re-run.

## consumers
- src/ir/integration.ts:6417 resolveFunc arm JSSTR_CHARCODEAT_FN → ensureHostCharCodeAtGuarded (7 cells)
- src/ir/integration.ts:6421 resolveFunc arm NATIVE_CHARCODEAT_FN → ensureNativeCharCodeAtHelper (24 cells)
- src/ir/integration.ts:6425-6436 resolveFunc arm NATIVE_FLATTEN_FN / NATIVE_FLAT_CHARCODEAT_FN (6 cells, 2 symbols each)
- src/ir/integration.ts:6437 resolveFunc arm JSSTR_CHARCODEAT_TRUSTED_FN → ensureHostCharCodeAtTrusted (2 cells)
- src/ir/integration.ts:6527 resolveFunc arm IR_STRING_CHAR_CODE_AT_FN (5 cells: 1 host, 4 native incl. probes)
- src/ir/integration.ts:6974 emitStringCharCodeAt adapter (provider branch 5/65; fallback 0/65)
- src/ir/lower.ts:2292 `string.char_code_at` case → emitter.emitStringCharCodeAt(…, instr.provider)
- src/ir/string-support.ts:70/137 unconditional semantic-provider attach `__ir_string_char_code_at`
- src/ir/integration.ts:7268 host pre-registration scan (JSSTR_CHARCODEAT_FN / TRUSTED_FN → addStringImports before Phase 3)
- src/ir/backend/linear-integration.ts:1511/1560/1637 linear twin (plan symbol + emit → `__linear_ir_str_char_code_at`)
- src/ir/from-ast.ts:8595/8661/8672+8784/7528/10116/10149/12908 producers (plan call, instr, proven read, hoist install)

## sites
- src/ir/integration.ts:5898 [lane-read] const native = ctx.nativeStrings;  (stringMethodPlan)
- src/ir/integration.ts:5907 [lane-read] funcName: native ? "__str_charCodeAt" : "__jsstr_charCodeAt", indexArgRep: "i32", padOmitted: "charcode-zero"
- src/ir/integration.ts:5964 [helper] preferLegacyFlatSubstringCharCodeAt(receiver: ts.Expression) { … declaration.initializer.expression.name.text === "substring"
- src/ir/integration.ts:6002 [lane-read] charReadPlan() { if (ctx.nativeStrings) { if (ctx.anyStrTypeIdx < 0 || …) return null; return { hoist: { flattenFuncName: NATIVE_FLATTEN_FN, readFuncName: NATIVE_FLAT_CHA
- src/ir/integration.ts:6011 [lane-read] if (ctx.standalone || ctx.wasi || ctx.strictNoHostImports) return null; return { hoist: null, trustedFuncName: JSSTR_CHARCODEAT_TRUSTED_FN };
- src/ir/integration.ts:6417 [consumer] } else if (ref.binding.kind === "intrinsic" && symbol === JSSTR_CHARCODEAT_FN) { index = ensureHostCharCodeAtGuarded(ctx);
- src/ir/integration.ts:6421 [consumer] } else if (ref.binding.kind === "intrinsic" && symbol === NATIVE_CHARCODEAT_FN) { index = ensureNativeCharCodeAtHelper(ctx);
- src/ir/integration.ts:6425 [consumer] (symbol === NATIVE_FLATTEN_FN || symbol === NATIVE_FLAT_CHARCODEAT_FN) … ensureNativeStringHelpers(ctx); index = symbol === NATIVE_FLATTEN_FN ? nativeStrHelperHandle(ctx,
- src/ir/integration.ts:6437 [consumer] } else if (ref.binding.kind === "intrinsic" && symbol === JSSTR_CHARCODEAT_TRUSTED_FN) { index = ensureHostCharCodeAtTrusted(ctx);
- src/ir/integration.ts:6527 [lane-read] } else if (… symbol === IR_STRING_CHAR_CODE_AT_FN) { index = ctx.nativeStrings ? ensureNativeCharCodeAtHelper(ctx) : ensureHostCharCodeAtGuarded(ctx);
- src/ir/integration.ts:6974 [adapter] emitStringCharCodeAt(_inputEncoding, provider) { if (provider) return [{ op: "call", funcIdx: resolver.resolveFunc(provider) }]; const idx = ctx.nativeStrings ? ensureNat
- src/ir/integration.ts:7268 [helper] instr.kind === "call" && instr.target.binding.kind === "intrinsic" && (symbol === JSSTR_CHARCODEAT_FN || symbol === JSSTR_CHARCODEAT_TRUSTED_FN) → usesStringOp = true  (h
- src/ir/integration.ts:7435 [helper] instrUsesStrings: … instr.kind === "string.char_code_at" ||
- src/codegen/char-code-at-helpers.ts:47 [record] export const JSSTR_CHARCODEAT_FN = "__jsstr_charCodeAt"; (:51 NATIVE_CHARCODEAT_FN = "__str_charCodeAt"; :76 NATIVE_FLATTEN_FN; :78 NATIVE_FLAT_CHARCODEAT_FN; :80 JSSTR_C
- src/codegen/char-code-at-helpers.ts:177 [producer] const charCodeAtIdx = ctx.jsStringImports.get("charCodeAt"); const lengthIdx = ctx.jsStringImports.get("length"); if (… undefined) return null;  (ensureHostCharCodeAtGuar
- src/codegen/char-code-at-helpers.ts:233 [producer] const flattenIdx = ctx.funcMap.get("__str_flatten") ?? ctx.nativeStrHelpers.get("__str_flatten"); … (ensureNativeCharCodeAtHelper — (ref $AnyString,i32)->f64, flatten + a
- src/codegen/char-code-at-helpers.ts:332 [producer] export function ensureNativeFlatCharCodeAtHelper(ctx) — (ref null $AnyString,i32)->i32, unguarded, no flatten
- src/codegen/char-code-at-helpers.ts:371 [producer] export function ensureHostCharCodeAtTrusted(ctx) { const charCodeAtIdx = ctx.jsStringImports.get("charCodeAt"); … (externref,i32)->i32, ONE record
- src/ir/string-support.ts:70 [helper] case "string.char_code_at": return irIntrinsicFuncRef(IR_STRING_CHAR_CODE_AT_FN);  (irStringCallableProviderRef — attached UNCONDITIONALLY by attachIrStringSupport :137)
- src/ir/string-runtime.ts:47 [record] export const IR_STRING_CHAR_CODE_AT_FN = "__ir_string_char_code_at";
- src/ir/runtime-host-capabilities.ts:286 [record] funcRecord("string.char_code_at", "wasm:js-string", "charCodeAt", ["externref", "i32"], ["i32"]),
- src/ir/dialect/js.ts:646 [record] export interface IrInstrStringCharCodeAt extends IrInstrBase { readonly kind: "string.char_code_at"; value; index; inputEncoding; provider? }  (:23 JS-dialect verdict lis
- src/ir/nodes.ts:2292 [record] | IrInstrStringCharCodeAt  (union member; :2650/:2819/:2931 walkers)
- src/ir/lower.ts:2292 [consumer] case "string.char_code_at": { emitValue(instr.value, out); emitValue(instr.index, out); emitter.emitStringCharCodeAt(instr.inputEncoding, out, instr.provider);
- src/ir/from-ast.ts:8539 [record] charCodeAt: { hostArgs: [{ kind: "f64" }], result: irVal({ kind: "f64" }), requiredArgs: 0 },  (STRING_METHOD_TABLE)
- src/ir/from-ast.ts:8595 [producer] if (methodName === "charCodeAt" && cx.resolver?.preferLegacyFlatSubstringCharCodeAt?.(receiverExpr) === true) { return null; }
- src/ir/from-ast.ts:8661 [producer] return cx.builder.emitStringCharCodeAt(recv, index, evidence.receiverEncoding);  (evidence path → string.char_code_at instr; only with receiver encoding evidence)
- src/ir/from-ast.ts:8672 [producer] const plan = cx.resolver?.stringMethodPlan?.(methodName, args.length, receiverEncoding) ?? null; … :8784 cx.builder.emitCall(irIntrinsicFuncRef(funcName), loweredArgs, ca
- src/ir/from-ast.ts:7528 [producer] const provenCharRead = matchProvenCharRead(expr, cx.provenCharReads); if (provenCharRead !== null) return emitUnary("f64.convert_i32_s", emitProvenCharReadI32(…))
- src/ir/from-ast.ts:10116 [producer] function emitProvenCharReadI32 … proof.hoist ? emitCall(irIntrinsicFuncRef(proof.hoist.readFuncName), [flat, index]) : emitCall(irIntrinsicFuncRef(proof.trustedFuncName!)
- src/ir/from-ast.ts:10149 [producer] const plan = cx.resolver?.charReadPlan?.() ?? null; if (!plan) return null;  (installCanonicalCharReadProof — called for EVERY for-statement before shape detection)
- src/ir/from-ast.ts:12908 [producer] const provenCharRead = matchProvenCharRead(inner, cx.provenCharReads);  (i32-pure leaf path)
- src/ir/backend/linear-integration.ts:1560 [adapter] if (method === "charCodeAt") { return { funcName: LINEAR_IR_STRING_CHAR_CODE_AT_FN, indexArgRep: "i32", padOmitted: "charcode-zero" };
- src/ir/backend/linear-integration.ts:1637 [adapter] emitStringCharCodeAt(inputEncoding?) { … bindLinearStringRuntime(memoryPlan, { intrinsic: "char-code-at", inputEncoding }); return [{ op: "call", funcIdx: resolveRuntimeF
- src/ir/backend/linear-integration.ts:683 [adapter] stringEq: STRING_EQ_POLICY_DISABLED, stringLen: STRING_LEN_POLICY_DISABLED,  (policy block a charCodeAt policy joins)
- src/codegen/stdlib-selfhost.ts:510 [adapter] stringEq: STRING_EQ_POLICY_DISABLED, stringLen: STRING_LEN_POLICY_DISABLED,
- src/codegen-linear/runtime.ts:2532 [record] export const LINEAR_IR_STRING_CHAR_CODE_AT_FN = "__linear_ir_str_char_code_at";
- src/ir/char-read-loop.ts:13 [helper] for (let i = <int ≥ 0>; i < recv.length; i++) // … recv.charCodeAt(i) …  (pure AST recogniser; no lane read)
- src/ir/runtime-manifest.ts:565 [record] if (kind === "host-capability" || kind === "host-managed") { if (family === "native") throw … "mixes host and native async providers"  (host-capability kind is bound to t
- src/ir/runtime-manifest.ts:1728 [record] if (provider.implementation.kind === "host-capability" && provider.hostCapabilities.length === 0) throw … must request at least one host capability
- tests/issue-3931.test.ts:101 [test] expect(wat).toContain("$$slot___cca_flat") / not.toContain("f64.const nan") / not.toContain("i64.")  (shape pins, all 5 configs)
- tests/issue-1105-charcodeat.test.ts:38 [test] describe("#1105 nativeStrings charCodeAt") — NaN OOB, ToIntegerOrInfinity, no i32-pure fold of OOB read
- tests/issue-3526-string-boundary-schema.test.ts:94 [test] const STILL_UNPROVIDED_IDS = NEW_IDS.filter(…)  (:378 expect(STILL_UNPROVIDED_IDS).toHaveLength(4); :204 row("string.char_code_at") whole-shape; :313 ABI vs registration)
- tests/issue-3520-callable-preregistration.test.ts:13 [test] export function __jsstr_charCodeAt(value: number): number { return value + 2; }  (source-unit lookalike must NOT resolve as the intrinsic)
- tests/string-derived-length-fast-path.test.ts:344 [test] const helperStart = wat.indexOf("(func $__str_charCodeAt"); expect(helperStart).toBe(-1);  (const-substring receiver stays on legacy inline arm)
- tests/linear-charcodeat-ascii-fast-path.test.ts:142 [test] describe("linear backend: charCodeAt ASCII fast path")
- tests/issue-3521-prepared-component-dependencies.test.ts:994 [test] kind: "string.char_code_at",

## fixtures
- LOOP: `export function hashStr(s: string): number { let h = 0; for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) | 0; return h; }`
- LOOPSUM: `export function sum(s: string): number { let n = 0; for (let i = 0; i < s.length; i++) { n = n + s.charCodeAt(i); } return n; }`
- LOOPNC: `export function sumnc(s: string): number { let n = 0; for (let i = 0; i < s.length - 1; i++) { n = n + s.charCodeAt(i); } return n; }  (non-canonical bound → guarded)`
- READ: `export function read(s: string, i: number): number { return s.charCodeAt(i); }`
- CONST: `export function first(s: string): number { return s.charCodeAt(0); }`
- NEG: `export function neg(s: string): number { return s.charCodeAt(-1); }`
- OOB: `export function oob(): number { return "abc".charCodeAt(10); }  (literal receiver → encoding evidence → string.char_code_at INSTR path)`
- OMIT: `export function omit(s: string): number { return s.charCodeAt(); }`
- TPL: `export function tpl(a: string): number { return `${a}!`.charCodeAt(0); }`
- FOROF: `export function forof(s: string): number { let n = 0; for (const c of s) { n = n + c.charCodeAt(0); } return n; }`
- CHAIN: `export function chain(s: string): number { return s.trim().charCodeAt(0); }`
- SUBCONST: `export function sub(s: string): number { const t = s.substring(1, 3); return t.charCodeAt(0); }  (preferLegacyFlatSubstringCharCodeAt)`
- CLEAN: `export function clean(a: number, b: number): number { return a + b; }  (control)`

## matrix
| fixture | lane | ok | bytes | sha | imports | reaches | demoted |
|---|---|---|---|---|---|---|---|
| LOOP | gc-host | y | 313 | d05f4670b197 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:charReadPlan/host-trusted=1 fromast:provenRead-i32pure/trusted=1 resolve:6437/host-tr | - |
| LOOP | gc-native-strings | y | 22601 | cbda921b0e74 | - | plan:charReadPlan/native-hoist=1 fromast:provenRead-i32pure/hoist=1 resolve:6425/native-fl | - |
| LOOP | standalone | y | 22765 | 346ffb3ec606 | - | plan:charReadPlan/native-hoist=1 fromast:provenRead-i32pure/hoist=1 resolve:6425/native-fl | - |
| LOOP | wasi | y | 22792 | 8d4b1f93715b | - | plan:charReadPlan/native-hoist=1 fromast:provenRead-i32pure/hoist=1 resolve:6425/native-fl | - |
| LOOP | linear | N | 0 | e3b0c44298fc | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | COMPILE-FAIL: Unsupported method call: . |
| LOOPSUM | gc-host | y | 309 | df45ced9493b | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:charReadPlan/host-trusted=1 fromast:provenRead/trusted=1 resolve:6437/host-trusted=1 | - |
| LOOPSUM | gc-native-strings | y | 22601 | 454cd76972de | - | plan:charReadPlan/native-hoist=1 fromast:provenRead/hoist=1 resolve:6425/native-flatten=1  | - |
| LOOPSUM | standalone | y | 22765 | 343462d0d0ba | - | plan:charReadPlan/native-hoist=1 fromast:provenRead/hoist=1 resolve:6425/native-flatten=1  | - |
| LOOPSUM | wasi | y | 22792 | 3d55a511e5e8 | - | plan:charReadPlan/native-hoist=1 fromast:provenRead/hoist=1 resolve:6425/native-flatten=1  | - |
| LOOPSUM | linear | N | 0 | e3b0c44298fc | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | COMPILE-FAIL: Unsupported method call: . |
| LOOPNC | gc-host | y | 377 | c9c4c55f80b5 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:charReadPlan/host-trusted=1 plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_cha | - |
| LOOPNC | gc-native-strings | y | 22682 | 953996eb95be | - | plan:charReadPlan/native-hoist=1 plan:stringMethodPlan/native=1 fromast:planCall/__str_cha | - |
| LOOPNC | standalone | y | 22846 | 1d5550e114dd | - | plan:charReadPlan/native-hoist=1 plan:stringMethodPlan/native=1 fromast:planCall/__str_cha | - |
| LOOPNC | wasi | y | 22873 | 3d1637923b3a | - | plan:charReadPlan/native-hoist=1 plan:stringMethodPlan/native=1 fromast:planCall/__str_cha | - |
| LOOPNC | linear | N | 0 | e3b0c44298fc | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | COMPILE-FAIL: Unsupported method call: . |
| READ | gc-host | y | 264 | fd53a89b1672 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| READ | gc-native-strings | y | 22529 | 40541defedcb | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| READ | standalone | y | 22693 | dea8c631c182 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| READ | wasi | y | 22720 | 9e6fc9f350e5 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| READ | linear | y | 5621 | 5aed38e456dd | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | - |
| CONST | gc-host | y | 307 | 8a5e851b1ffd | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| CONST | gc-native-strings | y | 22610 | 75a73a3e60af | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CONST | standalone | y | 22774 | 207ed9ee5571 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CONST | wasi | y | 22801 | 1579e26c7393 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CONST | linear | y | 5629 | 3981de70d06d | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | - |
| NEG | gc-host | y | 301 | e4d2a64e74e1 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| NEG | gc-native-strings | y | 22606 | b4df8ee2cf16 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| NEG | standalone | y | 22770 | a2e4743ee4fa | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| NEG | wasi | y | 22797 | 107f3394fff8 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| NEG | linear | y | 5626 | b8b135115d3a | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | - |
| OOB | gc-host | y | 324 | bf9d0060e0b9 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | fromast:emitStringCharCodeAt/ir-instr=1 resolve:6527/ir-host-guarded=1 emit:adapter/provid | - |
| OOB | gc-native-strings | y | 22191 | 414ad53b55ba | - | fromast:emitStringCharCodeAt/ir-instr=1 resolve:6527/ir-native-guarded=1 emit:adapter/prov | - |
| OOB | standalone | y | 22806 | f8648808d5a0 | - | fromast:emitStringCharCodeAt/ir-instr=1 resolve:6527/ir-native-guarded=1 emit:adapter/prov | - |
| OOB | wasi | y | 22833 | 8d088efc3433 | - | fromast:emitStringCharCodeAt/ir-instr=1 resolve:6527/ir-native-guarded=1 emit:adapter/prov | - |
| OOB | linear | y | 5659 | 4dfc57f9da66 | - | fromast:emitStringCharCodeAt/ir-instr=1 linear:emit/linear-runtime=1 | - |
| OMIT | gc-host | y | 304 | 94943e2f2e66 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| OMIT | gc-native-strings | y | 22608 | 8d5be65ec9f2 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| OMIT | standalone | y | 22772 | f1bb8d9dab77 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| OMIT | wasi | y | 22799 | 763010600fae | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| OMIT | linear | y | 5618 | 008d5a4ae250 | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 (compile repo | - |
| TPL | gc-host | y | 366 | c98938c569dd | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| TPL | gc-native-strings | y | 22651 | 81c41cbd8d0d | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| TPL | standalone | y | 23432 | 72be3e23af7d | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| TPL | wasi | y | 22842 | 96a420b5341a | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| TPL | linear | N | 0 | e3b0c44298fc | - | linear:plan/linear-runtime=1 fromast:planCall/__linear_ir_str_char_code_at=1 | COMPILE-FAIL: Unsupported method call: . |
| FOROF | gc-host | y | 1574 | fe6aefefc3e2 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 | - (no charCodeAt site reached; legacy compiled) | forof:method-call-unsupported — 'method  |
| FOROF | gc-native-strings | y | 22849 | 1ca184a4a6af | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| FOROF | standalone | y | 49299 | e25320b97c23 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| FOROF | wasi | y | 49326 | b0d5e125349f | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| FOROF | linear | N | 0 | e3b0c44298fc | - | - | COMPILE-FAIL: Unsupported method call: . |
| CHAIN | gc-host | y | 345 | 94f7a78f834e | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 env.string_trim#2 | plan:stringMethodPlan/host=1 fromast:planCall/__jsstr_charCodeAt=1 resolve:6417/host-guard | - |
| CHAIN | gc-native-strings | y | 22668 | 3820370b75d3 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CHAIN | standalone | y | 22832 | d919a455b0d4 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CHAIN | wasi | y | 22859 | eda341491031 | - | plan:stringMethodPlan/native=1 fromast:planCall/__str_charCodeAt=1 resolve:6421/native-gua | - |
| CHAIN | linear | N | 0 | e3b0c44298fc | - | - | COMPILE-FAIL: Unsupported method call: . |
| SUBCONST | gc-host | y | 319 | 430379a78449 | wasm:js-string.length#0 wasm:js-string.charCodeAt#1 env.string_substring#2 | plan:preferLegacyFlatSubstring/true=2 fromast:preferLegacy-null/demote-legacy=2 | sub:method-call-unsupported (legacy inli |
| SUBCONST | gc-native-strings | y | 22586 | 031a6f575094 | - | plan:preferLegacyFlatSubstring/true=2 fromast:preferLegacy-null/demote-legacy=2 | sub:method-call-unsupported |
| SUBCONST | standalone | y | 22750 | dbc3db32001b | - | plan:preferLegacyFlatSubstring/true=2 fromast:preferLegacy-null/demote-legacy=2 | sub:method-call-unsupported |
| SUBCONST | wasi | y | 22777 | a01f3ae38bbe | - | plan:preferLegacyFlatSubstring/true=2 fromast:preferLegacy-null/demote-legacy=2 | sub:method-call-unsupported |
| SUBCONST | linear | N | 0 | e3b0c44298fc | - | - | COMPILE-FAIL: Unsupported method call: . |
| CLEAN | gc-host | y | 119 | a734964356e5 | - | - | - |
| CLEAN | gc-native-strings | y | 21977 | 07067e4c76dc | - | - | - |
| CLEAN | standalone | y | 22592 | 23cc3ba48737 | - | - | - |
| CLEAN | wasi | y | 22619 | c05c31ff8cfc | - | - | - |
| CLEAN | linear | y | 4878 | eab66857b0be | - | - | - |
