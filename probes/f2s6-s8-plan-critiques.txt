
===== CRITIQUE 2: F2-S7 — charCodeAt under manifest policy (draft plan section
verdict: REVISE BEFORE FILING — structure and design are sound and census-faithful, but five numbers are untraceable or wrong (+407 → +353; +265 → +260; churn quoted as net; +2/+4/+4 → +1/+1/+1; the #5274 'expect 0' inference), the compare/eq/len pin edit is mis-described (field-by-field expects, not an object-literal gain), three F2-S5-head anchors are mis-mapped (:7268→:7362 not :7364; :1012/:1128 are concat's lines, not len's), Contract A misses the `numberBoundaryProvider` union widening, and test (f) names a policy-injection harness that does not exist. Fix those and it is ready.
-- anchorErrors (10)
   * Site 7 `:7268 → :7364`: on the F2-S5 head the pre-registration block's `if (` is :7361 and `instr.kind === "call" &&` is :7362; :7364 is the `instr.target.binding.symbol === JSSTR_CHARCODEAT_FN ||` line. The a07f side cites the `instr.kind` line, the branch side cites the symbol line — off by two. Cite :7362 or give the content anchor (the `if (instr.kind === "call" && … JSSTR_CHARCODEAT_FN || … [...]
   * Contract C item 9: `integrationStringLenPolicy` `:970` (→ after F2-S5's `:1012`) and the freeze policy `:1068-1074` → `:1128`: on the branch :1012 is `function integrationStringConcatPolicy` and :1128 is `stringConcatDemand: irStringConcatDemand(…)`; the len twins are at :972 and :1123. As written it reads as if the len anchors moved to :1012/:1128. Say 'after F2-S5's concat policy `:1012` / concat demand [...]
   * Contract F, frozen-default pins 'compare `:268`, eq `:301`, len `:350` (+2/+4/+4 after F2-S5)': measured on b814a01a4c the shifts are +1/+1/+1 (compare :269, eq :302, len :351) — F2-S5 adds one import line above and appends its `stringConcat` expect AFTER the `stringLen` line. And the pins are field-by-field `expect(frozen.policy.stringLen).toEqual(STRING_LEN_POLICY_DISABLED)` lines (compare.test.ts:259-262 [...]
   * Concat suite 'defaults block (`:366-373` on the branch)': the `it("defaults an omitted policy closed…")` spans :362-376; the field expects are :366-374 with `stringLen` at :374 (the line the new expect follows). :366-373 stops one line short.
   * Schema fence: 'a07f :378 toHaveLength(4); 3 after F2-S5 — re-anchor from the branch, F2-S5 reshaped that block': on the branch `PROVIDED_IDS` is still :93 (`["string.concat","string.eq","string.len"]`) and the fence is still :378 (`toHaveLength(3)`); nothing at those anchors was reshaped (F2-S5's 13/23 schema churn is at :77 NEW_IDS, :152, :194, :212 concat row). Drop 'reshaped'.
   * `tests/issue-1105-charcodeat.test.ts:38-110`: the file is 108 lines (describe at :38). Inherited from the census; cite :38-108.
   * `charReadPlan` `:6002-6012`: `charReadPlan() {` is :6003 (:6002 is the trailing doc-comment line); the branch anchor :6078 is right. Trivial, but the F2-S5-head mapping in the table (`:6002-6012 → :6078`) pairs a comment line with a code line.
   * Adapters `linear-integration.ts:683` / `stdlib-selfhost.ts:510`: at a07f those are the `stringEq:` lines; on the F2-S5 head the blocks are :683-686 / :510-513 with `stringConcat: STRING_CONCAT_POLICY_DISABLED,` at :686 / :513 — the line the new field follows. Also give full paths once: `src/ir/backend/linear-integration.ts`, `src/codegen/stdlib-selfhost.ts`, and `src/codegen/char-code-at-helpers.ts` (the intro [...]
   * P4 'integration.ts passes such as `batchStringConcat`': it is `src/ir/passes/batch-string-concat.ts` (imported at integration.ts:305 on both trees, invoked at a07f :3681/:3683 → branch :3735/:3737), not an integration.ts pass. Wording only.
   * `preparedStringLenProvider` (`:382-395`): the function spans :382-403; :395 is its `host-callable` branch. Trivial.
-- numberErrors (5)
   * 'F2-S5's branch is +407 with two features': not traceable. The branch's own frontmatter grant (plan/issues/3526-…md:225-226 on 6d6425c8e3) records +353 net src LOC against 7f998ff8, and `git diff --numstat a07f65319f b814a01a4c -- src/` sums to +353. Use +353.
   * 'F2-S3 was +265 with a resolve arm': copied from the F2-S4 plan (:5259). The F2-S3 checkpoint (:4560) and its frontmatter grant (:166) record +260. (+265 is F1-S4's figure, :112.)
   * Intro '+138 integration.ts, +172 runtime-manifest.ts, +67 intrinsic-support.ts, +26 intrinsics.ts': those are `--stat` churn (insertions+deletions), not net. numstat: integration 116/22 (+94 net), runtime-manifest 168/4 (+164), intrinsic-support 66/1 (+65), intrinsics 26/0 (+26); the branch also touches linear-integration.ts and stdlib-selfhost.ts (+2 each), omitted from the list.
   * '+2/+4/+4 after F2-S5' for the compare/eq/len pins → measured +1/+1/+1 (see anchorErrors).
   * P3 '#5274 rewrote the 17 reds in five suites, so expect 0 there': the five suites (#5274 table: issue-320, imported-string-constants, issue-3529-equivalence-error-imports, issue-3529-dataflow-outcomes, issue-3529-ir-producer-parity) are none of the suites P3 lists as its control set, so 'expect 0 there' says nothing about that set. The control set's red count on the lane's base is unmeasured — say only that, [...]
-- conventionErrors (5)
   * Contract A item 4 omits widening `numberBoundaryProvider`'s `id`/`feature` parameter unions (runtime-manifest.ts:723-738); F2-S5 had to add `StringConcatRuntimeProviderId` / `StringConcatRuntimeFeature` there and the two charCodeAt rows will not typecheck without the same edit. Name it (it is where the F2-S5 branch's helper already lists concat).
   * Test (a) 'the async projection excludes both by id': the len precedent (len.test.ts:280-295) pins the capability RECORD via `asAsyncHostAdapter`'s seven-ID filter; provider ROWS are dropped from `projectRuntimeBackendRequirements` by KIND (:565-585 `continue`s past anything but host-capability/host-managed/native-managed). Say which: records `string.char_code_at` + `string.len` excluded by id; the two `runtime- [...]
   * LOC grant: the lane branches from F2-S5, so the issue file it edits already carries the F2-S5 grant block (:225+ on the branch); say the F2-S7 block goes after it, not just 'the F2-S4 block at :187-221 is the template'.
   * Contract F 'gain `stringCharCodeAt: { charCodeAt: "unsupported" }`' names the wrong edit shape for the compare/eq/len pins (field-by-field expects, see anchorErrors) — the house convention is a named edit per pin; give the exact appended line.
   * Item 11 'its `null` on missing imports keeps today's meaning': state the meaning — a null index falls to resolveFunc's `unknown-function-ref` invariant at :6556-6561 ('cannot materialize callable provider …'); the len suite's analogous pin is 'fails closed on a missing host import rather than registering a new one' (:540-544).
-- designObjections (4)
   * Test (f) 'a hand-built freeze with policy `native` and a function carrying a `__jsstr_charCodeAt` call throws `selection-preparation-mismatch` at resolve': no entry point exists for a test to inject a manifest policy into `resolveFunc` — the policy is derived from ctx inside compile() (`integrationStringCharCodeAtPolicy`), and the only adapters that pass a policy (linear :683, stdlib-selfhost :510) pass [...]
   * Site 1's `index` may be null from BOTH helpers (`ensureNativeCharCodeAtHelper` is also `number | null`, char-code-at-helpers.ts:227); the plan treats only the host null. Harmless (same :6556 invariant), but the (d) pin 'no funcMap' plus 'fail-closed on missing import' should cover both arms.
   * P1: the TPL cell (`${a}!`.charCodeAt(0), census: carries `env.__concat_3#5` on gc-host) is the one charCodeAt cell F2-S5 could plausibly touch bytes on — worth naming as the cell to look at first if the 65/65 BEFORE re-run differs, rather than the generic 'any difference is a finding about F2-S5'.
   * No objection to the core choices — they match the census verdict: `runtime-callable` for both rows (validation :1716-1760 permits capabilities; `host-capability` is async-bound at :565; `host-callable`/`exactCallableImportIndex` is the wrong routine for a defined helper), the two-producer demand (35 plan-path + 4 instr cells), verify-not-redecide at sites 2/3, no second `attachIrStringSupport` pass (F2-S4 [...]

===== CRITIQUE 3: F2-S6 — batched many-arity string concat under manifest poli
verdict: REVISE before filing. The measured basis is sound and matches the census verdict on the three falsified claims (`!ctx.wasi` live in the host selector only; :137/:138 move, :378 does not; live-regime flushes exist but at no `__concat_N` site), the F2-S5 branch anchors (+54/+75 line drift, names) are accurate, and the sha/import-list pins check out. What must change: (1) the schema-suite pin claims — :173 MOVES (two-way kind branch) and :163 KINDS moves, both unnamed; (2) test pins must be cited by real filenames (issue-1470:21 currently points at a different file's line); (3) the pre-registration story names the wrong function (`preregisterCallableProviders` walks instructions and is where the arms already mint; the kind-skip loop is `ADMITTED_CALLABLE_TARGETS`) and P3 names a codegen import catalog instead of the manifest-record consumers; (4) `RuntimeProvider.signature` belongs to runtime-manifest.ts and the family signature is unverified unless `RUNTIME_FEATURE_SIGNATURES` is widened — decide (a) vs census option (b); (5) numbers: F2-S5 measured +353 not the +170 estimate, 84 vs 85 cells, the 'five F2-S5 fence rows reproduce' claim is untraceable (F2-S5's fence pins CAT3 and TPL), LOC baseline moved since grounding, `min ≥ 1` contradicts the measured floor 3; (6) the func-id list choice reopens the typed-half hole F2-S2 closed — pick a family-id list or justify. Sequencing (stack on F2-S5, enqueue after merge; F2-S7 stacks; F2-S8 last), zero-yield statement, and the 'IR pass + two arms only, legacy twins stay' scope statement are correct and should stay as written.
-- anchorErrors (10)
   * Test pins named by shorthand do not resolve to files, and one resolves to the WRONG file: `issue-1470:21` → tests/issue-1470.test.ts:21 EXISTS but is `compile(source, { fileName: "issue-1470.ts", target: "wasi" })`; the census pin is tests/issue-1470-standalone-string-imports.test.ts:21 (`/^env::__concat_\d+$/`). Likewise `issue-958` → tests/issue-958-concat-chain.test.ts, `issue-3523-ir-calendar`/`-algorithms` [...]
   * `tests/issue-3526-string-boundary-schema.test.ts:173 / :409 hold by construction` — FALSE for :173. Its body (:174-186) is a two-way kind branch: `if (record.kind === "func") {…} else { expect(isRuntimeHostCapabilityGlobalId(record.capability)).toBe(true); …record.field.scheme }` — a `func-family` record falls into the else arm and fails. The census verdict lists `:173/:409 (records must cover every id)` among [...]
   * Unnamed moving pin: tests/issue-3526-string-boundary-schema.test.ts:163 `expect([...RUNTIME_HOST_CAPABILITY_KINDS]).toEqual(["func", "global"])` — a new record kind moves it, and `RUNTIME_HOST_CAPABILITY_KINDS` (src/ir/runtime-host-capabilities.ts:134) is not in Contract E's edit list either.
   * `src/emit/resolve-layout.ts:91`, `STABLE_FUNC_BASE = 1<<21` — :91 is `export function inLiveShiftRange(...)`; `STABLE_FUNC_BASE = 1 << 21` is :80 (census says :80).
   * `target-profile.ts:68-69` — path is src/target-profile.ts (there is no src/codegen/target-profile.ts); content (`if (input.wasi && input.standalone) throw`) verified at :68-69. Cite the full path.
   * `RuntimeProvider.signature` (Contract B.6, design bullet 3) — the field is `signature?: IntrinsicSignature` on `RuntimeProviderDefinition` at src/ir/runtime-manifest.ts:545, i.e. section A, not intrinsic-support.ts/intrinsics.ts. The readers the widening actually touches are `RUNTIME_FEATURE_SIGNATURES: Partial<Record<RuntimeFeature, IntrinsicSignature>>` (:655) and the `signatureEquals` check under `provider- [...]
   * `preregisterCallableProviders` (src/ir/integration.ts:7084) `must skip them by continue` — it does not iterate provider rows by kind at all; it walks instruction buffers, calls `resolveAndObserveCallableProvider` on every runtime/intrinsic callable ref (:7118-7120), and ends with `flushLateImportShifts(ctx, null)` (:7130). The `continue`-on-kind loop the draft describes is `ADMITTED_CALLABLE_TARGETS` in [...]
   * P3 names `catalogProgramAbiCallableImports` as a `RUNTIME_HOST_CAPABILITY_RECORDS` consumer — it is src/codegen/program-abi-import-planning.ts:911 and catalogs ctx imports, not manifest records. Real record/kind consumers to enumerate: `canonicalizeRuntimeHostCapabilityCatalog` (runtime-host-capabilities.ts:463, called runtime-manifest.ts:1661), `asCallableRuntimeHostCapabilityRecord` (:453-460, throws on `kind [...]
   * `the numberBoundaryProvider id/feature unions` (Contract A.3) — `numberBoundaryProvider` (runtime-manifest.ts:722) is a row factory; the unions are `RuntimeProviderId` (branch :503-514, one member per family) and `RuntimeFeature` (:68), plus the `RUNTIME_FEATURES`/`RUNTIME_PROVIDER_IDS` arrays (branch :1394/:1408).
   * P2's 'the string.const census measured two prepare passes per compile' refers to `prepareStrings` scan events (`[f]` then `[<module-init>]`, census-string-const.md line 80), not to manifest freezes. `prepareBuiltFnRuntimeManifest` is called once, at integration.ts:3918 (main) / :3993 (F2-S5 branch), inside the same `compileIrPathFunctions` (:1808) that runs the batch pass at :3678 / :3732 — pass precedes freeze [...]
-- numberErrors (5)
   * `F2-S5 +170 estimate` used as the LOC analogy base — F2-S5's checkpoint note on the branch (plan file line 5720, 5827 at 6d6425c8e3) MEASURED +353 net src LOC ('not the plan's +170 estimate'). Cite +353 (measured) beside F2-S4's +381; the +450–550 guess should be re-based on two measured slices, not one estimate.
   * `84/84 cells … the wasi edge cell included` (V-A) and P1 'all 84 cells … plus the wasi edge cell' — 84 = 14 fixtures × 6 lanes; with the edge cell it is 85. Pick one count.
   * `F2-S5's five fence rows (CAT3/CAT4/TPL3/TPLEQ/CAT) reproduce sha-for-sha` — not in the F2-S6 census (it mentions F2-S5 only as a forward reference, lines 35/74/79/93/94/106). F2-S5's actual fence test (branch tests/issue-3526-string-boundary-concat.test.ts:460-492) pins TWO gc-host cells: CAT3 149/4677a84a2dcd and TPL (`${a}!`) 173/a6702c76db07 — TPL is not an F2-S6 fixture. Say 'CAT3 gc-host reproduces [...]
   * `no src/ delta` between a07f65319f and 5f13a35bc6 is true, but `scripts/loc-budget-baseline.json` changed (40 lines) in that window (plus 6 test files: imported-string-constants, issue-320, 3517, 3529-*). The LOC_GATE_BASE ceilings the lane will be measured against are therefore NOT the grounding's — say so in the LOC paragraph.
   * Design bullet 1 says the id 'joins RUNTIME_HOST_CAPABILITY_FUNC_IDS … with one count change' (18→19) while Contract E specifies record validation `min ≥ 1` and section (a) pins `min 3` — the family floor is 3 at every measured site (pass :199, `irStringConcatManySymbol` RangeError `< 3` at string-runtime.ts:67-70, native MIN 3 at native-batched-concat.ts:20). `min ≥ 1` is a number nobody measured; use 3 or justify.
-- conventionErrors (6)
   * Every file:line must be usable by the implementing lane: replace the shorthand pin names in F ('Fences that must NOT move') and in V-B with the full tests/… filenames from the census (see anchorErrors #1).
   * Contract B.6 places `RuntimeProvider.signature` widening under intrinsic-support.ts/intrinsics.ts; the interface lives in runtime-manifest.ts (A). Move the item and add the `RUNTIME_FEATURE_SIGNATURES` / `signatureEquals` readers to A.
   * Existing-pins-that-move list omits schema :163 (KINDS) and :173-186 (kind branch) — the census verdict names :173; the template requires 'each named'.
   * The census verdict's third correction (live-regime flushes exist in the set: ASYNC gc-strict 26 refs/18 map, gc-native-strings 75 map) is reproduced correctly, but the draft's sentence 'none is a `__concat_N` registration' should keep the verdict's qualifier 'those lanes never mint it' so a reader does not infer the flush was measured AT a __concat_N site on a live-regime module (census 'unmeasured' #3 says it [...]
   * Sequencing paragraph is fine, but the intro should state that F2-S5 has no merged PR at writing (gh unavailable here; the branch tip is a squash + main merge) so 'enqueue only after F2-S5 merges' is a real wait, not a formality.
   * 'Census artifacts' bullet: `f2s6/f2s6-policy.mts` is a check:host-import-policy probe walker (reads scripts/check-host-import-policy.ts's `const probes = {`), `f2s6/f2s6-shift.mts` is the shift re-run driver — both verified present; but the draft should also say the shift figures come from f2s6-shift.mts + a re-created patch, per the verdict ('I re-created the shift patch and fixed the field; results then [...]
-- designObjections (6)
   * Putting `string.concat.many` into `RUNTIME_HOST_CAPABILITY_FUNC_IDS` makes `RuntimeHostCapabilityFuncId` (runtime-host-capabilities.ts:73) include it, so a plain `host-callable { capability: "string.concat.many" }` TYPE-CHECKS — exactly the hole F2-S2 closed for globals ('typed on the FUNC half … a compile error, not a lowering-time surprise', runtime-manifest.ts:480-486). The only guard left is the runtime [...]
   * 'The host row is never pre-registered … `preregisterCallableProviders` must skip them' misstates the mechanism (see anchorErrors #7). Today's late mint IS the resolve arm executing inside preregisterCallableProviders' walk, sealed by its trailing `flushLateImportShifts(ctx, null)`; the census's import-position facts (index 5 / 6 / 97, batchOpen=false except ASYNC) come from that walk. The design must say: [...]
   * Signature family is unverified by construction: `provider-signature-mismatch` fires only when `RUNTIME_FEATURE_SIGNATURES[feature]` exists (:1628 `expectedSignature !== undefined`), F2-S5 registered NO entry for `js.string.concat` (grep on branch: none), and the draft adds none for `js.string.concat.many`. A family `signature` on the two rows would then be checked by nothing — the 'closed contract' motive for [...]
   * Cross-policy rule `batch !== "off" && batch !== stringConcat.concat` can never fire from the projections: F2-S5's `integrationStringConcatPolicy` is `nativeStrings ? native : host` (branch :1012-1014) and `batch` is `native` only when nativeStrings, `host` only when !nativeStrings. It guards hand-built policies only — say that (one (b) pin, V-C's 'its one pin' is right) rather than presenting it as what makes [...]
   * V-C 'revert item 11 only → exactly the (d) batch-selection pins' is too optimistic: item 11 deletes the literal 8 and the four flag reads; reverting it also re-introduces batching on lanes where the policy says off only if the projection differs — it does not (verbatim) — so bytes stay identical and only source pins fail. Fine, but 'revert item 12 only → exactly the (d) arm pins + (e)' is wrong in the other [...]
   * (c) end-to-end pins duplicate F2-S5's fence for CAT3 gc-host (149/4677a84a2dcd, `env.__concat_3`) — say the two suites deliberately pin the same cell so a red is attributed to whichever slice landed last, or drop the duplicate; a silent duplicate reads as an unmeasured second fixture.

===== CRITIQUE 7: F2-S8 — string.const under manifest policy (draft plan secti
verdict: REVISE — the decision and honesty framing are right and every measured number traces to the census or to a named artifact, but the section cannot go in as-is: one file path is wrong in the direction opposite to the census correction (sink.ts must be porffor/sink.ts), two integration.ts scan anchors (:7247, :7292-7294) are off on this tree, the mint is mis-anchored at :1660 instead of :1670, `RUNTIME_FEATURES` does not exist, the func-only guard list misses :92 and becomes 7 sites after F2-S5, and the 'only integration.ts/runtime-manifest.ts move' warning omits intrinsic-support.ts, both adapters and the test pins that F2-S5 also shifts. Fix those, cite the F2-S5 tip 6d6425c8e3, rename 'sub-B' to the F2-S3-P1/F2-S4-P3 retirement precedent, flag booltpl.mts as worktree-hard-wired, name the concat defaults pin (:362 on the branch), and add the two P2 sub-questions (early-return dependency on the demand conjunction; `observe` after preregistration) before dispatch.
-- anchorErrors (13)
   * `src/ir/backend/sink.ts:401` — no such file. The Porffor sink IS under porffor/: `src/ir/backend/porffor/sink.ts:401` (census verdict: sink.ts is the one where the porffor/ directory is CORRECT; bytecode-emitter.ts:413 and contract-conformance.ts:96 are the ones directly under backend/). The draft applied the correction to the wrong file.
   * `integration.ts:7247` cited (item 11) as the string.const literal scan — :7247 is `let usesGenericStringRepeat = false;`. The scan line `if (instr.kind === "string.const") literals.add(instr.value);` is :7251 (the census's own :7247 is stale on this tree). Likewise `:7292-7294` for the extern.regex adds: :7292 is a comment, :7293 `usesStringOp = true`, the two `literals.add` are :7294-7295.
   * `import-collector.ts:1660` labelled '(finalize mint)' in table row 9, the 38-of-39 bullet and the After-this-slice table — :1660 is the `if (ctx.nativeStrings)` lane read; the `addStringConstantGlobal(ctx, value)` mint call is :1670 (census verdict correction :1671→:1670). Cite :1660 as the branch, :1670 as the mint.
   * Item 9 / P4 func-only guard list `:234`/`:279`/`:314` (+ async-runtime-providers.ts:106) is incomplete on main — `resolveRuntimeHostCapabilityFuncRecord` is also called at intrinsic-support.ts:92 (inside ADMITTED_CALLABLE_TARGETS, which the census lists) — and stale after F2-S5: the branch has SEVEN call sites (:94/:236/:281/:316/:358/:398/:449). P4 must grep the call sites, not enumerate three lines.
   * `ADMITTED_CALLABLE_TARGETS` (`:76-81`) — the const starts at :77 (:76 is blank); on the F2-S5 branch it is :79.
   * Item 2 names `RUNTIME_FEATURES` as a list to splice into — no such export exists in runtime-manifest.ts (grep: only *_RUNTIME_FEATURES id tuples). The feature/row plumbing that actually exists is `STRING_LEN_RUNTIME_PROVIDERS` (:948-962, the per-seam row array) spliced into `RUNTIME_PROVIDERS` (:1228), plus `RUNTIME_FEATURE_SIGNATURES` (:655) if a signature is published per feature. Name those.
   * `program-abi-global-planning.ts:98` cited for `PROGRAM_ABI_GLOBAL_ROLE.nativeStringLiteral` — :98 is `prepareNativeStringLiteral(value: GlobalDef)`; the role member is :14 (`nativeStringLiteral: 4`), used at :121. (Census carried the same imprecision.)
   * F2-S5 branch cited at `b814a01a4c` — that is the squash commit; the branch tip is `6d6425c8e3` (b814a01a4c + a merge of origin/main). The moved anchors the draft gives (prepareStrings :7329, storageForConst :7463, emitResolvedStringConst :6674, `if (usesStringOp && !ctx.nativeStrings) {` :7401, runtime-manifest.ts 2131 lines, schema :137=18, STILL_UNPROVIDED_IDS=3) all hold AT THE TIP — verified — so cite the tip.
   * Intro says only `integration.ts` and `runtime-manifest.ts` lines move when F2-S5 lands. F2-S5 also changes `src/ir/intrinsic-support.ts` (+67: the draft's :500→:553, :382→:384, :234→:236, :366→:368), `src/ir/intrinsics.ts` (+26, but appended after :287 so `:287-291` survives), `src/ir/backend/linear-integration.ts` (+2 — the draft's `:683-684` gains a `stringConcat` line), `src/codegen/stdlib-selfhost.ts` (+2 — [...]
   * '10 suites name `hostCapabilityRecords`' — 11 test files on main (boolean-boundary-intrinsic, boundary-residuals, generator-number-box, ir-runtime-manifest, number-boundary-intrinsics, string-boundary-compare/eq/len/schema, issue-4103, issue-4104).
   * `linear-integration.ts:739-745` — the `attachIrStringSupport` call with `storageForConst: () => undefined` is :741-744 inside the `providerBoundFunctions = usesRepeat ? … : functions` ternary spanning :739-746, and it runs ONLY when `usesRepeat`. Cite :739-746 and note the guard.
   * 'standalone TPLONLY 6 via F2-S6's batched helpers' — the batched `__str_concat_N` helper that interns the extra literals is EXISTING native codegen (census: 'batched __str_concat_N helper interns extra literals'); F2-S6 is the unlanded plan to govern it. Do not attribute today's bytes to F2-S6.
-- numberErrors (6)
   * All census-traceable numbers check out on the tree/census: 78 cells (13×6), 21 markers, 78/78 + 54 re-instrumented, 38/39 mints, 10/10 and 7/7 already:true, BOOLTPL thresholds 2→3 delta 1, 352/351 `addStringConstantGlobal(` grep lines, RUNTIME_HOST_CAPABILITY_IDS 18, ARRAY_NEW_FIXED_MAX 10000, funcIdx 2097203, `_43`/`_41`, intern counts ASCII 2 / DUP 3 / LONG12000 3+1 / TPLONLY standalone 6, F2-S4 +381 vs +150 [...]
   * '29/29 green ... measured on this tree' — traceable: `f2s8-red-recheck.out` ran at /home/user/js2 (HEAD f1739d2b52, which contains 5f13a35bc6 = #5465), issue-320 8 + imported-string-constants 21 = 29 passed. OK, but the plan should name the artifact (`scratchpad/f2s8-red-recheck.out`) and the sha it ran on, as it does for the census.
   * 'The 78 census cells must reproduce … EXCEPT where F2-S5/S6/S7 moved bytes — expected nowhere by their own V-A' — F2-S5's V-A is not on main and I did not verify a checkpoint on the branch; F2-S6/S7 have no V-A at all yet (plans only). Write 'expected nowhere IF their V-A held; any diff is a finding about the predecessor' — the draft already says the second half; drop 'by their own V-A' as evidence.
   * '+420 net src LOC' is an estimate, correctly marked as such; it is unmeasured and should say so explicitly in the LOC bullet (it currently reads as if derived).
   * P3 'N unmeasured' is honest and matches the census's unmeasured list. Good.
   * 'a lone surrogate with `.length` 3' — `"x\uD800y"` length 3, consistent with the LONE fixture.
-- conventionErrors (6)
   * 'F2-S3 sub-B pattern' (P3) — 'sub-B' is F1-S4 / F2-S1 vocabulary in the issue file (:3065 F1-S4 checkpoint, :3177 F2-S1 plan: 'one dead-fallback retirement (sub-B)'). The measured fallback-retirement precedents for family 2 are F2-S3's checkpoint P1 ('the emitter no-provider fallback is RETIRED, on a measurement') and F2-S4's checkpoint P3. Name those.
   * Artifacts: `booltpl.mts` is ALSO hard-wired to the removed worktree (`/home/user/js2/.claude/worktrees/verify-string-const/src/index.ts`) exactly like `matrix-wt.mts`; the draft flags only matrix-wt. probe3 imports `../src/index.js` (relative, runs from a worktree's `.tmp/`), probe2 likewise. Say which drivers need re-pointing.
   * Verified present and correctly named: census-string-const-{matrix.mts,instrument.py,matrix.md,matrix.json,probe.mts,probe2.mts,probe3.mts}, booltpl.mts, matrix-wt.mts; f2s6/{f2s6-matrix.mts,f2s6-instrument.py,matrix.md,matrix.json} (also f2s6-policy.mts, f2s6-shift.mts, matrix.out — unmentioned, fine); f2-cca-{matrix.mts,instrument.py,matrix-before.md,matrix-before.json}. probe2 does contain `/ab+c/i.test(s)`; [...]
   * Structure/order matches the F2-S5 template (intro → What moves (census, N cells) → design section → Contract A-F → P1-P5 with the BEFORE matrix as P1 → V-A..V-D + LOC/grant rule → Out of scope → After this slice). Both F2-S4 checkpoint corrections are honoured (const-only pass instead of attachIrStringSupport; V-C predicts the frozen-row-only-authority shape and defers the count). Issue links are plain #NNNN. [...]
   * Schema-suite `:22` comment quote ('no provider names any of the six new rows') is on main verbatim and still on the F2-S5 branch; the `:561` block is the F2-S2 'every func-assuming consumer fails closed' describe at :560 — fine.
   * `check:ir-only` is a package script but there is no `scripts/check-ir-only.mjs`; V-D lists it by script name, which is fine — just run via `pnpm run`.
-- designObjections (7)
   * Placement of `prepareStringConst` after `prepareStringLength` (:1089) puts it INSIDE the `runtime` branch — `if (!runtime) return { entries };` at :1060 returns before it. That is safe only because `stringConstDemand` joins the 'freeze nothing' conjunction (item 8), so every module with a string.const/extern.regex freezes. The plan should state this dependency explicitly (item 12 ↔ item 8) and pin it: a demand- [...]
   * Moving `materializerForConst` behind the freeze moves `ctx.programAbiCallableProviders.observe(provider, funcIdx)` (:7388-7389) to AFTER `prepareIrRuntimeManifest` / `preregisterCallableProviders`. P2 fences GLOBAL pushes; it does not ask whether the callable-provider registry still accepts `observe` after preregistration snapshots it, nor whether the intrinsic provider `__ir_string_literal_materialize:N` must [...]
   * `irStringConstDemand` counts `extern.regex` as demand (item 11), so a regex-only module freezes `js.string.const` and publishes `string.const` in `hostCapabilityRecords` — while nothing reads that row (regex pattern/flags emit through :6596 with no storage, item 13). The manifest then CLAIMS a capability the seam does not govern for that module. Coherent, but say it plainly in the honesty paragraph ('the frozen [...]
   * Row 6 / item 12 say `:7307-7315` is 'NOT touched' and stays in `prepareStrings`, but item 12 also has `prepareStrings` KEEP `providerForRepeat` and the carrier attach while the const arm becomes `() => undefined` — good. However the draft says the omnibus call's callable arm 'is not idempotent on a second pass' as the reason for the const-only pass; note that the FIRST pass (in prepareStrings) is the one [...]
   * Exact-global check `ref.binding.module === arm.module`: `programAbiStringConstantRef` (:991-1012) finds the import by walking `ctx.mod.imports` at the `stringGlobalMap` index, so the binding's `module` is whatever :175-177 derived — the check can only fail if `hasLoneSurrogate` in integration.ts disagrees with imports.ts's call. That is a real invariant (two derivations, one source `src/string- [...]
   * Design choices are picked and the alternatives named (two new kinds vs reusing `host-capability` — rejected on `projectRuntimeBackendRequirements` :565 async-family coupling, matching the charCodeAt census; nominal `EXTERNREF_GLOBAL_INTRINSIC_SIGNATURE` vs a `valueType` field; policy values host/native vs host-global/native-global; two features vs one row with two capabilities). No contradiction with a measured [...]
   * Sequencing is consistent with the brief (F2-S8 last; branch from origin/main after S5/S6/S7 merge, else predecessor stacking from the REAL branch). Fine.

===== FINAL 5: F2-S7 — charCodeAt under manifest policy (revised plan secti
-- decisions (7)
   * Accepted every anchorError after re-reading the trees: site 7 → :7362 with a content anchor; item 9 now says the len twins are :972/:1123 and the charCodeAt lines go after concat's :1012/:1128; compare/eq/len shifts are +1/+1/+1 (concat expect at :270/:303/:359 is the line the new expect follows); concat defaults block :362-376 / expects :366-374; schema :93/:378 unchanged on the branch [...]
   * Accepted every numberError: +353 (numstat sums to exactly +353, matches the branch grant :225-226); F2-S3 +260 (checkpoint :4560, grant :166; +265 is F1-S4 at :112); intro now quotes numstat insertions/deletions with net and adds the two adapter files; the #5274 'expect 0' inference is replaced by 'unmeasured' plus the one known red cited to f2-cca-pin-clean.out:20-28.
   * Accepted every conventionError: Contract A item 4 now leads with widening numberBoundaryProvider's id/feature unions (a07f :722-738, branch :788 where StringConcat* already sits); test (a) distinguishes records-excluded-by-ID (len.test.ts :280-295) from rows-ignored-by-KIND (:565-585); LOC grant goes after the branch's F2-S5 block; the pin edit is the exact appended expect line, the object- [...]
   * Accepted the (f) objection: verified no policy-injection entry point exists (only adapters pass a policy, both DISABLED with no demand; the family's sole mismatch pin, eq.test.ts:471, is source-text). Downgraded (f) to source-text pins, added probe P6 to look for a harness, and rewrote V-C so the behavioural-fence claims are conditional on P6.
   * Accepted the both-helpers-null point (char-code-at-helpers.ts:227 verified): item 11 and pin (d) now cover both arms.
   * Accepted the P1 TPL-first suggestion.
   * Kept everything the critique endorsed unchanged (runtime-callable for both rows, two-producer demand, verify-not-redecide, no second attach pass, zero yield, divergence-5 scoping). Nothing in the critique was found wrong, so no item was rejected.
-- openQuestions (4)
   * P6 is new and unmeasured: whether any test harness can freeze a charCodeAt policy and drive resolveFunc — decides whether (f) stays a source-text pin.
   * The standing-red control set for P3 on the lane's base is unmeasured; only the string-derived-length-fast-path red is known.
   * Reach counts and V-C failing counts must be re-measured on the F2-S5-stacked base (the census numbers are from a07f).
   * F2-S6 vs F2-S7 landing order is left to whichever is ready first; the second lands with a mechanical re-merge/re-anchor.

===== FINAL 6: F2-S6 — batched many-arity string concat under manifest poli
-- decisions (16)
   * Applied: all ten shorthand test pins replaced with the census's full filenames (verified each cited line in the named file; tests/issue-1470.test.ts:21 is a compile() call, the pin is tests/issue-1470-standalone-string-imports.test.ts:21).
   * Applied: schema suite :163 (KINDS) and :173-186 (two-way kind branch) now listed as moving pins; :135-136 sorted-union identity added as a moving pin because the three-way id union changes it.
   * Applied: RuntimeHostCapabilityFuncFamilyId in its own list RUNTIME_HOST_CAPABILITY_FUNC_FAMILY_IDS (critique design objection 1) — verified the typed-half hole: RuntimeHostCapabilityFuncId is (typeof RUNTIME_HOST_CAPABILITY_FUNC_IDS)[number] at :73, so the draft's placement would have let host-callable name the family id with only asCallableRuntimeHostCapabilityRecord's runtime throw (:456) [...]
   * Applied: pre-registration story rewritten — verified preregisterCallableProviders (:7084) walks instruction buffers, calls resolveAndObserveCallableProvider (:7118-7120) and seals with flushLateImportShifts(ctx, null) (:7130); the kind-skip is ADMITTED_CALLABLE_TARGETS :81, which already skips unknown kinds by positive check, so Contract B.8 is 'no edit, pin it'.
   * Applied: signature — picked census option (b), no signature family. Verified provider-signature-mismatch fires only when RUNTIME_FEATURE_SIGNATURES[feature] exists (:1628) and the F2-S5 branch has no js.string.concat entry in that table (grep at 6d6425c8e3), so a family signature would be unchecked. Alternative (a) named with its cost in the design bullet and the ranking table.
   * Applied: RuntimeProviderDefinition.signature readers moved to section A (verified :545, :655, :1280 signatureEquals, :1627-1636); B.6 removed, intrinsics.ts untouched.
   * Applied: cross-policy rule kept but stated as unreachable from projections (verified branch integration.ts:1012-1014) — it guards hand-built policies only; P4 rests on stringConcat.concat alone.
   * Applied: P2 rewritten around one compileIrPathFunctions (:1808) with pass :3678-3684 (fn: final at :3684) preceding the single freeze at :3918 (branch :3993); the 'two prepare passes' reference to the string.const census is disclaimed as prepareStrings scan events.
   * Applied: P3 consumer list replaced with the real record/kind consumers (canonicalize :463 ← :1661, asCallable :453-460, assert :359, async attachment :605-613/:653, async-plan.ts:442, async-runtime-providers.ts:120, IntrinsicRuntimeProviderImplementation runtime-manifest.ts:528); catalogProgramAbiCallableImports (:911) noted as not a consumer.
   * Applied numbers: F2-S5 +353 measured (branch plan lines 5720/5827) replaces the +170 estimate and the LOC guess is re-based to +400–500 (smaller than the draft because the signature family is dropped); 84 cells in matrix.md + a separate wasi edge cell = 85, stated explicitly (verified matrix.md has 84 data rows and no edge cell; f2s6-matrix.mts has no wasi/nativeStrings:false lane); the [...]
   * Applied: resolve-layout.ts:80 (STABLE_FUNC_BASE), src/target-profile.ts:68-69 full path, RuntimeProviderId/RuntimeFeature/RUNTIME_FEATURES/RUNTIME_PROVIDER_IDS named instead of 'numberBoundaryProvider unions', 'those lanes never mint it' qualifier restored, 'no merged PR at writing' added to the intro, shift-figure reproducibility (f2s6-shift.mts + re-created patch) and f2s6-policy.mts's [...]
   * Applied: (c) CAT3 gc-host pin explicitly marked as a deliberate duplicate of F2-S5's fence for attribution.
   * Kept against the critique: schema :409 'holds by construction' — canonicalizeRuntimeHostCapabilityCatalog sorts (the :407-408 reversed-input equality pin proves order-independence), so insertion order is irrelevant; the real precondition is that E's canonicalizer accepts the new kind, which the plan now states.
   * Kept against the critique (partly): the predicted V-C set for reverting item 12 is (d) arm pins + (e) — the critique's own description yields the same set; what was added is that bytes stay identical under that revert and that the (a) range-import pin is unaffected, with all counts marked unmeasured.
   * Corrected a critique misplacement: the critique writes IntrinsicRuntimeProviderImplementation as '(:528)' in the intrinsic-support.ts sentence; it is src/ir/runtime-manifest.ts:528 (grep: no hit in intrinsic-support.ts). The plan cites the right file.
-- openQuestions (5)
   * P4 remains open: whether isPreparedIrAsyncConcat (async-ir-planning.ts:679) can mint async.string.concat$arity5 on a lane where batch === 'off' (gc-native-strings / gc-strict); the plan's row selection on stringConcat.concat reproduces today's nativeStrings read either way, but the lane must record which lanes reach the async5 arm.
   * P2: whether healthyForLower at integration.ts:3918 (branch :3993) is the batched `final` for both the main entry and <module-init> — the demand scan's correctness depends on it and it was not measured by the census.
   * Order of 'func-family' inside RUNTIME_HOST_CAPABILITY_KINDS (:134) and the exact three-way union spelling of RUNTIME_HOST_CAPABILITY_IDS are left to the implementer; the moved schema pins (:135-136, :163) must match whatever E chooses.
   * LOC: unmeasured; the +400–500 figure is an analogy from two measured slices and the lane's real LOC_GATE_BASE will include F2-S5's post-merge baseline refresh.
   * Standing-red baseline count on the lane's base (P5) is unmeasured; #5465 rewrote the 17 #5274 pins after the census.

===== FINAL 8: F2-S8 — string.const under manifest policy (#3526, family 2,
-- decisions (27)
   * APPLIED (verified on tree): `src/ir/backend/sink.ts:401` -> `src/ir/backend/porffor/sink.ts:401`; bytecode-emitter.ts:413 and contract-conformance.ts:96 stay directly under backend/ (ls confirmed only sink.ts is under porffor/).
   * APPLIED: integration.ts literal scan is :7251 (`if (instr.kind === "string.const") literals.add(instr.value);`), regex block :7291 with `usesStringOp = true` :7293 and the two `literals.add` :7294-7295 (sed confirmed; :7247 is `let usesGenericStringRepeat`).
   * APPLIED: import-collector.ts lives at src/codegen/declarations/import-collector.ts; :1660 is the `if (ctx.nativeStrings)` branch, :1670 the `addStringConstantGlobal(ctx, value)` mint (grep confirmed). Cited as branch/mint everywhere (table row 9, 38-of-39 bullet, After table).
   * APPLIED AND EXTENDED the func-only-guard correction: on main there are SIX `resolveRuntimeHostCapabilityFuncRecord` call sites in intrinsic-support.ts (:92/:234/:279/:314/:356/:396), not the critique's four-plus-:92; seven on the F2-S5 branch (:94/:236/:281/:316/:358/:398/:449). Also corrected the draft's `async-runtime-providers.ts:106`: that line calls [...]
   * APPLIED: ADMITTED_CALLABLE_TARGETS starts at :77 on main (:79 on the branch); kind filter at :81.
   * APPLIED: `RUNTIME_FEATURES` does not exist (grep: only `*_RUNTIME_FEATURES` tuples). Item 2 now names the real plumbing: `FEATURE_SET` (:1243-1251, the feature-id set the draft meant), `RUNTIME_FEATURE_SIGNATURES` (:655), `STRING_LEN_RUNTIME_PROVIDERS` (:948-962) spliced into `RUNTIME_PROVIDERS` (:1228, spread at :1238).
   * APPLIED: PROGRAM_ABI_GLOBAL_ROLE.nativeStringLiteral is program-abi-global-planning.ts:14, used at :121; :98 is `prepareNativeStringLiteral` (kept as the native-resolver anchor in row 2).
   * APPLIED: cite the F2-S5 tip 6d6425c8e3 (b814a01a4c + merge of origin/main). All branch anchors re-verified at the tip: prepareStrings :7329, usesStringOp guard :7401, storageForConst :7463, observe :7483, emitResolvedStringConst :6674, resolveAndObserve :6379, lengthAttached :1143, RUNTIME_PROVIDERS :1369, isStringLenFeature branch :2048, 2131 lines, schema :137=18, STILL_UNPROVIDED_IDS=3, [...]
   * APPLIED: the intro now states the F2-S5 change set is 14 files (three-dot diffstat) and gives per-file deltas plus content anchors for intrinsic-support.ts, intrinsics.ts, both adapters, the schema suite, the defaults pins, issue-4104 and the kind-neutrality baseline (forof.string evidence :6531->:6629, confirmed in the branch diff).
   * APPLIED: 11 test files name `hostCapabilityRecords` on main (grep -l confirmed); listed in P4.
   * APPLIED: linear-integration.ts :739-746 is the `usesRepeat ? ... : functions` ternary; the attachIrStringSupport call with `storageForConst: () => undefined` is :741-744 and runs only when usesRepeat.
   * APPLIED: standalone TPLONLY 6 comes from the EXISTING batched `__str_concat_N` codegen (`src/codegen/native-batched-concat.ts:37`, the F2-S5 plan's own anchor); F2-S6 is named as the unlanded plan, not the producer.
   * APPLIED: the concat defaults pin is named — tests/issue-3526-string-boundary-concat.test.ts:362 on the F2-S5 branch only.
   * APPLIED: the 29/29 re-check names its artifact (`scratchpad/f2s8-red-recheck.out`, run at /home/user/js2 HEAD f1739d2b52; head of the file confirms `RUN v3.2.4 /home/user/js2`, `Tests 29 passed (29)`).
   * APPLIED: P1's 'expected nowhere by their own V-A' rewritten to 'expected IF their V-A held on their trees (F2-S5's is not on main; F2-S6/S7 are plans only)'.
-- openQuestions (7)
   * P3's N: how many times REGEX/gc-host reaches :6596 and REGEX/native :6589 — unmeasured; decides whether the emitter fallback retires in this slice or item 13 stands.
   * P2(ii): whether any observer OTHER than :6301/:6563/:7389 runs between prepareStrings (:3915) and prepareBuiltFnRuntimeManifest (:3918) — expected none; a positive answer would move the materializer's ordinal (still byte-fenced by LONG12000).
   * Whether F2-S6/F2-S7 (plans only, unlanded) add a closed `implementation.kind` enumeration in tests that the two new kinds must join (P4), and where their #selectProvider branches land relative to the isStringLenFeature branch (:1900-1914 main / :2048 branch) — re-anchor at dispatch.
   * The `addStringConstantGlobal(` grep drift (352 at a07f per census vs 353 on 5f13a35bc6 by `grep -rn ... src`): which grep the census used is not recorded; out-of-scope count only, but the checkpoint should pin one command.
   * V-C 'only the manifest rows' revert count — deliberately not predicted (F2-S4 divergence-5 shape); the checkpoint records it.
   * Whether check:ir-layering's allowlist admits a src/ir -> src/string-surrogate.ts import without a count change (P5 expects yes because it is not under src/codegen/; unverified against scripts/check-ir-layering.mjs).
   * Section length (493 lines) vs the 250-400 house guideline — needs a lead call on whether to keep the critique-mandated detail or apply the three cuts named in decisions.

=== 3526 issue file structure:
340071 plan/issues/3526-ir-r6-semantic-runtime-contract.md
2272:## 2026-09-01 F1-S3 pre-implementation verifications — Opus lane
2383:## 2026-09-01 F1-S3 implementation checkpoint — Opus lane
2638:## 2026-09-01 F1-S4 implementation plan — boundary residuals (family 1, slice 4)
2778:## 2026-09-01 F1-S4 pre-implementation verifications — Opus lane
2912:## 2026-09-01 F1-S4 implementation checkpoint — Opus lane
3170:## 2026-09-01 F2-S1 implementation plan — string.compare under manifest policy (family 2, slice 1)
3334:## 2026-09-01 F2-S1 checkpoint note — Opus lane
3612:## 2026-09-01 F2-S2 implementation plan — capability-record schema widening (family 2, slice 2)
3796:## 2026-09-01 F2-S2 checkpoint note — Opus lane
4054:## 2026-09-02 F2-S3 implementation plan — string.eq under manifest policy (family 2, slice 3)
4592:## 2026-09-02 F2-S5 implementation plan — string.concat under manifest policy (family 2, slice 5)
4932:## 2026-09-02 F2-S4 implementation plan — string.len under manifest policy (family 2, slice 4)
